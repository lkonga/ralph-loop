- [x] Fix baseline: resolve TypeScript errors and failing tests before continuing
# Ralph Loop V2 — Phase 1 Self-Fix PRD

> Being executed BY ralph-loop ON itself.
> Run `npx tsc --noEmit` after each change. Run `npx vitest run` if test files change.

## Tasks

- [x] In `src/prompt.ts`: Add a new section to the prompt output. After the "YOUR TASK TO IMPLEMENT" banner and task description, before the "MANDATORY" section, insert a block titled "ROLE & BEHAVIOR" containing: "You are an autonomous coding agent. Complete the task below by editing files directly. If you encounter errors, debug and fix them — do not stop. If tests fail, fix the tests or the code. When done, mark the checkbox in PRD.md and append what you did to progress.txt. Do not ask questions — act." Run `npx tsc --noEmit` and `npx vitest run` to verify — update test expectations in `test/copilot.test.ts` if they fail due to changed output format. Mark this checkbox [x] in PRD.md and append to progress.txt what you did.

- [x] In `src/prompt.ts`: Modify the `buildPrompt` function to accept a 4th optional parameter `maxProgressLines: number = 20`. When progressContent has more lines than maxProgressLines, keep only the LAST maxProgressLines lines and prepend a summary line like `[...N earlier entries omitted]`. This prevents the prompt from growing unboundedly as progress.txt grows. Run `npx tsc --noEmit` and `npx vitest run` — add a new test in `test/copilot.test.ts` that verifies truncation with 30 lines of progress. Mark this checkbox [x] in PRD.md and append to progress.txt what you did.

- [x] In `src/prompt.ts`: Change how PRD content is shown in the prompt. Instead of including the FULL PRD file content, filter it to show only unchecked task lines (lines matching `- [ ]`) plus a summary header like `Progress: N/M tasks completed`. Keep the markdown code fence wrapper. This reduces context waste since completed tasks aren't relevant. Run `npx tsc --noEmit` and `npx vitest run` — update tests in `test/copilot.test.ts` that check for PRD content format. Mark this checkbox [x] in PRD.md and append to progress.txt what you did.

- [x] Add a file `src/prompt.test.ts` (or use the existing `test/copilot.test.ts`) to add a test that verifies `buildPrompt` includes the "ROLE & BEHAVIOR" section in its output. Also add a test that when given 30 lines of progress content, the output contains `[...10 earlier entries omitted]` (since default maxProgressLines is 20). Also add a test that checked PRD lines (`- [x]`) are NOT included in the output but unchecked ones are. Run `npx vitest run` to verify all tests pass. Mark this checkbox [x] in PRD.md and append to progress.txt what you did.

---

## Phase 2 — Autopilot Patterns (from VS Code Copilot analysis)

> Patterns extracted from Justin Chen's autopilot implementation in vscode-copilot-chat.
> These are listed in priority order. Each task is self-contained.

### Nudge System (highest impact)

- [x] **Nudge on premature stop**: In `src/orchestrator.ts`, when the inactivity timeout fires but the PRD checkbox is NOT checked yet, instead of immediately yielding `TaskTimedOut` and moving on, first re-send the same task prompt via Copilot with a continuation nudge appended: `"Continue with the current task. You have NOT marked the checkbox yet. Do NOT repeat previous work — pick up where you left off. If you encountered errors, resolve them. If you were planning, start implementing."` Add a new event `LoopEventKind.TaskNudged` with `{ task, nudgeCount }`. Only THEN, if the nudge also times out, yield `TaskTimedOut`. Add a config `maxNudgesPerTask: number = 3` to `RalphLoopConfig`. Track `nudgeCount` per task. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Reset inactivity timer on file activity**: In `src/orchestrator.ts`, reset the inactivity timeout whenever the `FileSystemWatcher` detects a file change (the `onDidChange`/`onDidCreate`/`onDidDelete` events already exist for PRD watching). Currently the timeout is a fixed wall clock from task start. Instead, restart it on ANY watched file change — this prevents timing out on a task where Copilot is actively editing files but hasn't checked the box yet. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Enhanced Prompt (enriches existing Task 1)

- [x] **Anti-premature-termination checklist**: In `src/prompt.ts`, after the "ROLE & BEHAVIOR" section (added by Task 1), add a "DO NOT STOP IF" section: `"DO NOT STOP if: you encounter an error (debug and fix it), tests fail (fix them), you have remaining steps (complete them first), you have open questions (make a decision and proceed)."` Also add explicit ordering: `"When done: FIRST append what you did to progress.txt, THEN mark the checkbox in PRD.md. Both updates are required."` Also add persistence framing: `"Continue working until the task is fully complete. It's YOUR RESPONSIBILITY to finish. Do not hand back to the user."` Run `npx tsc --noEmit` and `npx vitest run`. Mark checkbox and append to progress.txt.

### Retry System

- [x] **Auto-retry with error classification**: In `src/orchestrator.ts`, when a task errors (caught by the existing try/catch), classify the error before giving up. Add `shouldRetry(error: Error, retryCount: number): boolean` — return `true` for transient errors (network, timeout) and `false` for fatal errors (user cancel via `stopRequested`, max iterations reached). Add `MAX_RETRIES_PER_TASK = 3` constant. In the catch block, if `shouldRetry` returns true, wait 2 seconds, decrement nothing, and re-enter the task body (re-send prompt). Track `retryCount` per task separately from `nudgeCount`. Add `LoopEventKind.TaskRetried` event. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### State Machine Enrichment

- [x] **Separate nudge/retry counters and latch pattern**: In `src/types.ts`, add `nudgeCount` and `retryCount` to `TaskState` (or create a new per-task tracking interface). Add `LoopEventKind.TaskNudged` and `LoopEventKind.TaskRetried` to the event union. In the orchestrator, when a nudge produces productive file changes (detected by the file watcher), reset `nudgeCount` to 0 — this allows the agent to be nudged again later if it stalls a second time. Once a task is verified complete (checkbox checked), set a `taskCompletedLatch` flag so it's never re-checked. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Testability

- [x] **Extract decision logic for testability**: Refactor `src/orchestrator.ts` to extract three pure methods: `shouldContinueLoop(state): boolean`, `shouldNudge(state): string | undefined`, `shouldRetry(error, retryCount): boolean`. These take plain state objects (no VS Code dependencies) and return decisions. Create `test/orchestrator.test.ts` with tests: (1) `shouldContinueLoop` returns false when all tasks done, true when tasks remain; (2) `shouldNudge` returns nudge text when task not complete and nudgeCount < max, undefined when at max; (3) `shouldRetry` returns true for transient errors under cap, false for fatal or over cap. Run `npx vitest run`. Mark checkbox and append to progress.txt.

### Hook System (V2 foundation)

- [x] **Hook type definitions**: In `src/types.ts`, define the hook system types: `type RalphHookType = 'SessionStart' | 'PreCompact' | 'PostToolUse' | 'TaskComplete'`. For each, define typed input interfaces (`SessionStartInput { prdPath: string }`, `TaskCompleteInput { taskId: string; result: 'success' | 'failure' }`, etc.) and a shared `HookResult { action: 'continue' | 'retry' | 'skip' | 'stop'; reason?: string; additionalContext?: string }`. Define `IRalphHookService` interface with one method per hook type. Export all types. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Hook integration in orchestrator**: In `src/orchestrator.ts`, accept an optional `IRalphHookService` in the constructor. At each yield point, call the corresponding hook method and check the returned `HookResult.action` — `'continue'` proceeds normally, `'retry'` re-enters the task, `'skip'` moves to next task, `'stop'` breaks the loop. If `additionalContext` is set, include it in the next prompt sent to Copilot. Create a default no-op implementation that always returns `{ action: 'continue' }`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

---

## Phase 3 — Extended Autopilot Patterns

> Additional patterns from VS Code Copilot autopilot. All are implementable in ralph-loop's architecture.

### Graceful Yield / External Stop Request

- [x] **External yield request**: In `src/types.ts`, add `yieldRequested: boolean` to `LoopOrchestrator` state. In `src/orchestrator.ts`, expose a `requestYield()` method that sets this flag. In the main loop, check `yieldRequested` after each task completes — if true, yield a new `LoopEventKind.YieldRequested` event and break the loop gracefully (not a hard stop). In autopilot mode, the yield is deferred: the loop continues until the current task's checkbox is checked, THEN yields. This mirrors VS Code's `yieldRequested` which autopilot ignores until `taskCompleted`. In `src/extension.ts`, wire a new VS Code command `ralph-loop.yield` that calls `requestYield()`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Dynamic Iteration Limits

- [x] **Auto-expand iteration limit in autopilot**: In `src/orchestrator.ts`, when `maxIterations` is reached but tasks remain, instead of immediately yielding `MaxIterations` and stopping, auto-expand the limit by 50% (capped at a hard maximum of 50 iterations). Add `hardMaxIterations: number = 50` to `RalphLoopConfig`. Log the expansion via `LoopEventKind.IterationLimitExpanded` (new event). Only auto-expand once per loop run — if the expanded limit is also reached, stop for real. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Shell Hook Execution

- [x] **External shell command hooks**: Extend `IRalphHookService` to support shell command hooks alongside in-process TS callbacks. Add a `ShellHookProvider` that executes a user-configured shell script on each hook event, passing hook input as JSON on stdin and reading `HookResult` as JSON from stdout. The script path is configured via `RalphLoopConfig.hookScript?: string`. Exit codes: 0 = success/continue, 1 = warning (log and continue), 2 = block/stop (halt the loop with the script's stderr as reason). This enables user-authored quality gates (e.g., run linter, check test coverage, validate git state) without modifying ralph-loop source. Add timeout of 30s per hook execution. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Prompt Enrichment Blocks

- [x] **Security and safety prompt blocks**: In `src/prompt.ts`, add optional prompt blocks that are injected when configured. Add `promptBlocks?: string[]` to `RalphLoopConfig` with possible values: `'security'` (OWASP Top 10 awareness, input validation, no hardcoded secrets), `'safety'` (prefer reversible actions, confirm destructive ops, don't delete files without PRD instruction), `'discipline'` (minimal changes, no over-engineering, no unsolicited refactoring), `'brevity'` (concise output, no verbose explanations in comments). Each block is a 2-3 sentence instruction appended after the "ROLE & BEHAVIOR" section. Default: `['safety', 'discipline']`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Tool Capability Discovery

- [x] **Deferred tool/capability awareness**: In `src/prompt.ts`, add a "AVAILABLE CAPABILITIES" section to the prompt that lists what the agent CAN do based on configuration. When hooks are enabled, mention it: `"Quality hooks are active — your work will be validated after each tool use."` When shell hooks are configured, mention the script: `"External validator: [script path] will run on task completion."` When specific prompt blocks are active, list them. This gives the agent awareness of the guardrails around it, similar to how VS Code's `nonDeferredToolNames` ensures the model knows about critical tools. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Agentic Proxy Pattern

- [x] **Copilot hook registration as agentic proxy**: Instead of polling the filesystem to detect task completion, register ralph-loop as a Copilot chat hook provider using the `ChatHookCommand` proposed API. Create `src/hookBridge.ts` that: (1) generates a small Node.js script at runtime (written to a temp file) that listens for hook invocations from Copilot on stdin, (2) registers this script as a `Stop` hook via VS Code's `chat.hooks` configuration so Copilot calls it when the agent wants to stop, (3) the script checks whether the PRD checkbox was actually marked — if not, returns `{ resultKind: 'error', stopReason: 'Task not complete — checkbox not marked in PRD.md' }` which makes Copilot continue working, (4) when the checkbox IS marked, returns `{ resultKind: 'success' }` letting Copilot stop normally. Also register a `PostToolUse` hook that resets the inactivity timer on each tool call. This replaces filesystem polling with event-driven completion detection. Note: requires `vscode.proposed.chatHooks` API — gate behind a feature flag `useHookBridge: boolean = false` in config. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Session identity tracking via proposed API**: In `src/extension.ts`, when available, read `vscode.window.activeChatPanelSessionResource` to get the URI of the active chat session. Store this in the orchestrator as `currentSessionId`. Use it to: (1) correlate which chat session ralph-loop fired a prompt into, (2) detect when the session changes (user opened a different chat) and pause the loop, (3) target hook registration to the specific session. Gate behind a feature flag `useSessionTracking: boolean = false` since this requires `vscode.proposed.chatParticipantPrivate`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Permission level escalation**: When firing prompts to Copilot, if the proposed `chatParticipantPrivate` API is available, set `permissionLevel: 'autopilot'` on the chat request. This enables Copilot's internal autopilot mode: auto-approves tool calls, auto-continues on stalls, requires explicit `task_complete` tool call before stopping. Ralph-loop's nudge system becomes a second layer on top of Copilot's built-in autopilot — ralph handles task sequencing while Copilot handles within-task autonomy. Gate behind `useAutopilotMode: boolean = false`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

---

## Phase 4 — Agentic Proxy Patterns (from SearchSubagentToolCallingLoop analysis)

> Patterns extracted from the agentic proxy implementation: ProxyAgenticSearchEndpoint, SearchSubagentToolCallingLoop, subagent invocation lifecycle, and background pipeline architecture.

### Subagent Task Model

- [x] **Invocation ID threading**: In `src/types.ts`, add `taskInvocationId: string` (UUID) to each task's runtime state. Generate it in `src/orchestrator.ts` when a task starts (before the prompt is sent). Thread it through: progress.txt entries, events, hook invocations, and any telemetry. This enables correlating "which task produced which changes" across the entire system. Similar to VS Code's `subAgentInvocationId` + `parentRequestId` pattern that links subagent calls to parent requests for tracing. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Isolated task conversation**: In `src/copilot.ts`, when opening a new Copilot chat for each task, ensure a FRESH chat session is used (no conversation history from previous tasks). Currently ralph fires `workbench.action.chat.open` — verify this opens a new session. If it reuses the panel, add `workbench.action.chat.newChat` before the prompt send. This mirrors the search subagent's pattern of creating a fresh `Conversation` with no parent history — each task should start clean without context pollution from prior tasks. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Forced conclusion nudge on iteration limit**: In `src/prompt.ts`, add a `buildFinalNudgePrompt(task, nudgeCount, maxNudges)` function that generates a "wrap it up" message when `nudgeCount >= maxNudges - 1`: `"Your remaining time is almost up. Produce your final result NOW: commit any partial work, update progress.txt, and mark the checkbox. If tests fail, document the failure and mark done anyway."` This mirrors VS Code's search subagent `isLastTurn` nudge: "your allotted iterations are finished — produce answer now." The orchestrator calls this for the final nudge instead of the standard continuation nudge. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Endpoint / Backend Abstraction

- [x] **Task execution strategy interface**: In `src/types.ts`, define `ITaskExecutionStrategy` with one method: `execute(task: PrdTask, prompt: string, options: ExecutionOptions): Promise<ExecutionResult>`. Create two implementations: (1) `CopilotCommandStrategy` (current approach — fire workbench command, poll filesystem), (2) `DirectApiStrategy` (future — call Copilot's API directly via proposed `chatProvider` API, get streaming response). Add `executionStrategy?: 'command' | 'api'` to `RalphLoopConfig`, defaulting to `'command'`. The orchestrator resolves the strategy from config and delegates. This mirrors VS Code's `ChatEndpoint` base class where `ProxyAgenticSearchEndpoint` swaps the backend by overriding `urlOrRequestMetadata`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Synthetic model awareness**: In `src/prompt.ts`, add a `modelHints` section to the prompt when configured. If `RalphLoopConfig.modelHint?: string` is set (e.g., `'claude-sonnet'`, `'gpt-4o'`), include model-specific behavioral instructions: Claude gets "use artifacts for long code", GPT gets "use code blocks". This mirrors VS Code's `ProxyAgenticSearchEndpoint` constructing synthetic `IChatModelInformation` — ralph doesn't know which model Copilot uses, but the user can hint it for prompt optimization. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

### Background Pipeline (future foundation)

- [x] **Task parallelization with DAG dependencies**: In `src/types.ts`, add optional `dependsOn?: string[]` to `PrdTask` (parsed from PRD.md indentation or explicit `depends: task-1` annotations). In `src/orchestrator.ts`, modify `pickNextTask` to return ALL tasks whose dependencies are met (not just the first one). When multiple independent tasks are ready, execute them in parallel — open multiple Copilot chats simultaneously. Add `maxParallelTasks: number = 1` to config (default sequential). This mirrors VS Code's background pipeline where reviewers run in parallel (up to 20) with a `maxConcurrencyPerStage` cap. Emit `LoopEventKind.TasksParallelized` with the task list. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **SubagentStop hook — completion verification gate**: In `src/hookBridge.ts` (created in Phase 3), extend the `Stop` hook to be a full verification gate. When Copilot's agent calls `task_complete`, the hook runs a verification checklist before allowing the stop: (1) PRD checkbox is marked, (2) progress.txt was updated, (3) no TypeScript compilation errors (`npx tsc --noEmit` exit code 0), (4) no test failures (`npx vitest run` exit code 0). If any check fails, return `{ resultKind: 'error', stopReason: 'Verification failed: [details]' }` which makes Copilot continue working on the failures. This mirrors VS Code's `SubagentStop` hook that can block stopping with reasons. Gate behind `useVerificationGate: boolean = false`. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

- [x] **Config-gated feature system**: In `src/types.ts`, add a `features` object to `RalphLoopConfig` that groups all boolean feature flags: `{ useHookBridge: boolean, useSessionTracking: boolean, useAutopilotMode: boolean, useVerificationGate: boolean, useParallelTasks: boolean }`. All default to `false`. In `src/extension.ts`, read these from VS Code settings under `ralph-loop.features.*`. In `src/orchestrator.ts`, check the feature object before activating each capability. This mirrors VS Code's `ConfigType.ExperimentBased` pattern where each consumer gets its own independent toggle. Run `npx tsc --noEmit`. Mark checkbox and append to progress.txt.

---

## Phase 5 — Deterministic Loop Hardening & Validation Hooks

> Patterns extracted from autopilot research (Tony Cheng, VS Code Copilot Chat), giocaizzi/ralph-copilot pipeline, and the Ralph ecosystem analysis.
> **TDD is MANDATORY**: Every task MUST follow red-green-refactor. Write failing tests FIRST.
> Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> See `ISSUES.md` for future improvement tracking.

### A — TDD & Mandatory Verification Gate

- [x] **Mandatory TDD gate in loop prompt and verification hook**: In `src/prompt.ts`, inside `buildPrompt` (line ~91), after the `'DO NOT STOP if: ...'` paragraph and before the `'When done: FIRST append...'` line, insert a new section titled `TDD GATE — MANDATORY` containing: `"Every task MUST follow the TDD cycle: (1) Write a failing test FIRST that covers the acceptance criteria, (2) Run npx vitest run — confirm the new test FAILS (red), (3) Implement the minimum code to make the test pass, (4) Run npx vitest run — confirm ALL tests PASS (green), (5) Refactor if needed — run tests again to confirm still green, (6) Run npx tsc --noEmit — must exit 0 with zero errors, (7) ONLY THEN mark the checkbox in PRD.md. If ANY test is red or tsc reports errors, you are NOT done — fix them before proceeding. This is not optional. No checkbox may be marked while tests are failing."` This block must be unconditional — not gated behind a feature flag or promptBlocks config; it is always emitted. In `src/hookBridge.ts`, in the `generateStopHookScript` function (lines ~9–85), make the verification gate always active by removing the `USE_VERIFICATION_GATE` conditional — checks 2–4 (progress.txt freshness, `npx tsc --noEmit`, `npx vitest run`) must always run regardless of config. Additionally, improve failure reporting: change the tsc failure message from the generic `'TypeScript compilation errors: ' + (tsc.stderr || 'see tsc output')` to capture and include stdout as well (tsc emits diagnostics to stdout), and change the vitest failure message to capture both stdout and stderr so specific failing test names are visible in the `stopReason`. Update the `runCommand` helper to return `{ ok, stdout, stderr }` instead of just `{ ok, stderr }`, then use `stdout` in the failure detail strings. In `src/types.ts`, remove `useVerificationGate` from the `RalphFeatures` interface and from `DEFAULT_FEATURES` since TDD is now mandatory and non-configurable. In `test/copilot.test.ts`, add a test that verifies `buildPrompt` output contains the string `'TDD GATE'` and the phrase `'Write a failing test FIRST'`. Add a test that verifies the stop hook script source always includes the tsc and vitest checks regardless of the `useVerificationGate` parameter value. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

- [x] **Multi-verifier system with DSL configuration**: In `src/types.ts`, add `type VerifierFn = (task: Task, workspaceRoot: string) => Promise<VerifyCheck>`, `interface VerifierConfig { type: string; args?: Record<string, string>; stages?: string[] }`, `interface VerificationTemplate { name: string; verifiers: VerifierConfig[] }`, and add `verifiers?: VerifierConfig[]`, `verificationTemplates?: VerificationTemplate[]`, and `autoClassifyTasks?: boolean` to `RalphConfig` (default `autoClassifyTasks` to `false`). In `src/verify.ts`, refactor `verifyTaskCompletion` to return `Promise<VerifyCheck[]>`. Create a `VerifierRegistry` class with `register(type: string, fn: VerifierFn)` and `get(type: string)`. Register built-in verifiers: `checkbox` (existing PRD checkbox check extracted), `tsc` (runs `npx tsc --noEmit`, passes on exit 0), `vitest` (runs `npx vitest run`, passes on exit 0), `fileExists` (checks `args.path` exists), `fileContains` (reads `args.path` and checks for `args.content`), `commandExitCode` (runs `args.command`, passes on exit 0), and `custom` (runs `args.command` as shell). Add `runVerifierChain(task, workspaceRoot, configs, registry, logger): Promise<VerifyCheck[]>` that runs ALL configured verifiers (no short-circuit) and returns composite results. Add `resolveVerifiers(task, config, registry): VerifierConfig[]` that: (1) uses explicit `config.verifiers` if set, (2) matches `verificationTemplates` by task category, (3) falls back to default `[{ type: 'checkbox' }, { type: 'tsc' }]`. When `autoClassifyTasks` is true, append verifiers based on keyword matching in task description (e.g., "test" → vitest). Write tests FIRST in `test/verify.test.ts`: checkbox verifier, fileExists verifier, fileContains verifier, commandExitCode verifier, runVerifierChain composite, resolveVerifiers defaults/overrides/auto-classify, allChecksPassed with chain. Mock `child_process.execFile` for tsc/vitest verifiers. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass. Write tests for new verifier types FIRST (TDD).

- [x] **Composable circuit breaker system**: Create `src/circuitBreaker.ts` with a `CircuitBreaker` interface: `{ name: string; check(state: CircuitBreakerState): CircuitBreakerResult }` where `CircuitBreakerState` aggregates runtime signals (nudge count, retry count, elapsed time, file changes, error history) and `CircuitBreakerResult` is `{ tripped: boolean; reason?: string; action: 'continue' | 'retry' | 'skip' | 'stop' | 'nudge' }`. Implement five pure-function breakers: (1) `MaxRetriesBreaker` — refactored from `decisions.ts`, trips at `retryCount >= maxRetries` (default 3), action `'stop'`; (2) `MaxNudgesBreaker` — trips at `nudgeCount >= maxNudges`, action `'stop'`; (3) `StagnationBreaker` — trips when `consecutiveNudgesWithoutFileChanges >= threshold` (default 2), action `'skip'`, uses self-resetting counter (productive work resets); (4) `ErrorRateBreaker` — trips when transient error rate exceeds threshold (0.6) in sliding window (5 iterations), action `'stop'`; (5) `TimeBudgetBreaker` — trips when elapsed > `timeBudgetMs` (default 600s), action `'skip'`. Implement `CircuitBreakerChain` that runs all breakers and returns FIRST tripped result (priority order) or `{ tripped: false, action: 'continue' }`. Add `circuitBreakers?: CircuitBreakerConfig[]` to `RalphConfig` where `CircuitBreakerConfig = { name: string; enabled: boolean; [key: string]: unknown }`. Provide `createDefaultChain(config): CircuitBreakerChain` with `['maxRetries', 'maxNudges', 'stagnation']` enabled, `errorRate` and `timeBudget` disabled by default. In `src/orchestrator.ts`, call `chain.check(state)` at each decision point — before nudge, before retry, before next iteration. Add `LoopEventKind.CircuitBreakerTripped` event. Define optional `onTrip` callback type for future human checkpoint integration but do NOT implement it yet. All breakers must be pure functions. Write tests FIRST in `test/circuitBreaker.test.ts`: each breaker's trip/no-trip conditions, chain priority ordering, disabled breaker skipping, createDefaultChain composition. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

### B — Determinism via Validation Hooks

- [x] **Pre-completion validation hook chain**: In `src/types.ts`, add `'PreComplete'` to `RalphHookType`. Add `PreCompleteInput { taskId: string; taskInvocationId: string; checksRun: VerifyCheck[]; prdPath: string; previousResults?: PreCompleteHookResult[] }` where `PreCompleteHookResult` extends `HookResult` with `{ hookName: string }`. Add `onPreComplete(input: PreCompleteInput): Promise<HookResult>` to `IRalphHookService`. Add `PreCompleteHookConfig { name: string; type: 'builtin' | 'shell' | 'custom'; command?: string; enabled: boolean }` and `preCompleteHooks?: PreCompleteHookConfig[]` to `RalphConfig` with default `[{ name: 'prd-checkbox-check', type: 'builtin', enabled: true }, { name: 'progress-updated', type: 'builtin', enabled: true }]`. In `src/orchestrator.ts`, after verifiers pass but BEFORE `TaskComplete` hook fires, execute the PreComplete chain: iterate enabled hooks in order, passing accumulated `previousResults`. If any returns `action: 'retry'`, re-enter task loop. If `action: 'stop'`, yield `Stopped` immediately (short-circuit). If `action: 'continue'`, proceed to next hook. Update `NoOpHookService` to include `onPreComplete`. In `src/hookBridge.ts`, integrate PreComplete into the Stop hook script — run `prd-checkbox-check` and `progress-updated` (mtime within 5 min) before allowing stop. Add `DEFAULT_PRE_COMPLETE_HOOKS` constant. Write tests FIRST in `test/orchestrator.test.ts`: all-continue proceeds to TaskComplete, retry causes re-entry, stop yields Stopped, previousResults accumulates, disabled hooks skipped. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

- [x] **Post-task diff validation with state passing and human checkpoint escalation**: Create `src/diffValidator.ts` exporting `DiffValidator` with `validateDiff(workspaceRoot, taskInvocationId): Promise<DiffValidationResult>` that runs `git diff --stat HEAD` and `git diff --name-only HEAD`. Return `{ filesChanged, linesAdded, linesRemoved, hasDiff, summary }`. When `hasDiff` is false and `requireChanges` is true, return nudge: `"No code changes detected. Review the task requirements and make the necessary code modifications."` Add config `diffValidation?: { enabled: boolean; requireChanges: boolean; maxDiffLines?: number; generateSummary: boolean }` to `RalphConfig` (default `{ enabled: true, requireChanges: true, generateSummary: true }`), add `maxDiffValidationRetries: number` (default 3). Add `LoopEventKind.HumanCheckpointRequested` event `{ task, reason, failCount, taskInvocationId }` and `LoopEventKind.DiffValidationFailed` `{ task, nudge, attempt, taskInvocationId }`. In orchestrator, after TaskCompleted, call `DiffValidator.validateDiff()` when enabled. Empty diff escalation: (1st) inject nudge and re-enter task (autopilot pattern), (2) yield DiffValidationFailed each attempt, (3) after maxDiffValidationRetries yield HumanCheckpointRequested and pause. When `generateSummary` and diff is non-empty, append structured state block to progress.txt: `### Task {id} State | Files: [{list}] | Lines: +{add}/-{rem} | Status: pass`. In `src/extension.ts`, on HumanCheckpointRequested show `vscode.window.showWarningMessage` with options: Continue, Skip Task, Stop Loop, Provide Guidance (opens input box → instructions injected as additionalContext). Support `maxDiffLines` truncation. Write tests FIRST: DiffValidator with mocked git, structured state format, nudge logic, orchestrator re-entry, human checkpoint after N failures. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

### C — Parallel Subagent-Style Execution

- [x] **Review-after-execute pattern**: In `src/types.ts`, add `ReviewAfterExecuteConfig { enabled: boolean; mode: 'same-session' | 'new-session'; reviewPromptTemplate?: string }` (default `{ enabled: false, mode: 'same-session' }`), add to `RalphConfig`. Add `LoopEventKind.TaskReviewed = 'task_reviewed'` with `{ task, verdict: ReviewVerdict, taskInvocationId }` where `ReviewVerdict = { outcome: 'approved' | 'needs-retry'; summary: string; issues?: string[] }`. In `src/copilot.ts`, export `sendReviewPrompt(taskDescription, mode, reviewPromptTemplate, logger)` — in `same-session` mode reuse current chat context; in `new-session` mode call `startFreshChatSession` first. Default review template: `"Review the changes just made for the following task: [TASK]. Check for: correctness, code quality, potential bugs, security issues. Provide a structured verdict: APPROVED or NEEDS-RETRY with critical issues."` In `src/orchestrator.ts`, after TaskCompleted and TaskComplete hook, if `reviewAfterExecute.enabled`, build review prompt, call `sendReviewPrompt`, capture output, append to progress.txt, yield TaskReviewed. If verdict is `needs-retry`, re-enter task loop. Read config from VS Code settings. Write tests FIRST: no review event when disabled, review emitted when enabled, needs-retry causes re-entry. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

- [x] **Parallel monitor for in-flight task observation**: In `src/types.ts`, add `maxConcurrencyPerStage: number` to `RalphConfig` (default 1) — mirrors VS Code's `maxConcurrencyPerStage` from SearchSubagentToolCallingLoop. Add `ParallelMonitorConfig { enabled: boolean; intervalMs: number; stuckThreshold: number }` (default `{ enabled: false, intervalMs: 10000, stuckThreshold: 3 }`) and `parallelMonitor?: ParallelMonitorConfig` to config. Add `LoopEventKind.MonitorAlert = 'monitor_alert'` with `{ alert, taskId }`. In `src/orchestrator.ts`, create `startMonitor(taskId, taskInvocationId): { stop: () => void }` that runs `setInterval` at `intervalMs` checking: PRD and progress.txt mtime, progress.txt growth, checkbox transitions. Track `staleIntervalCount` — increment when no signals change, reset on change. When `staleIntervalCount >= stuckThreshold`, yield MonitorAlert and trigger early nudge. Start before `execute()`, stop after completion (both success/error paths). When `maxConcurrencyPerStage > 1` AND `features.useParallelTasks`, use it as concurrency cap in `pickReadyTasks()`. Write tests FIRST: alert after threshold, counter reset on progress change, clean stop, not started when disabled, maxConcurrencyPerStage overrides maxParallelTasks. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

### D — Context Preservation

- [x] **PreCompact session reset with consistency checker**: In `src/types.ts`, add `PreCompactBehavior { enabled: boolean; summaryMaxLines: number; injectGitDiff: boolean; injectProgressSummary: boolean }` (default `{ enabled: true, summaryMaxLines: 50, injectGitDiff: true, injectProgressSummary: true }`), add to `RalphConfig`. Add `LoopEventKind.ConsistencyCheckPassed` and `LoopEventKind.ConsistencyCheckFailed` events with `{ phase, checks, failureReason? }`. Add `IConsistencyChecker` interface with `runDeterministic(input: ConsistencyCheckInput): Promise<ConsistencyCheckResult>` and `runLlmVerification(input): Promise<ConsistencyCheckResult>` (LLM method is stub, returns skip). In `src/hookBridge.ts`, implement `generatePreCompactHookScript(prdPath, progressPath, config)` — generates Node.js script that on context compaction reads progress.txt (last N lines), optionally runs `git diff --stat`/`--name-only`, outputs `HookResult` with `action: 'continue'` and `additionalContext` containing structured resumption block: `"=== SESSION RESUMPTION CONTEXT ===\n## Progress So Far\n[summary]\n## Recent File Changes\n[diff]\n## Current Task\n[unchecked task]\n=== END ==="`. Register as PreCompact hook alongside existing Stop/PostToolUse hooks. Create `src/consistencyChecker.ts` implementing `DeterministicConsistencyChecker` with three checks: (1) checkbox state matches expected phase, (2) progress.txt mtime within 5 min, (3) file paths mentioned in task description exist on disk. In `src/orchestrator.ts`, accept optional `IConsistencyChecker`, call `runDeterministic` after task execution. Add `useLlmConsistencyCheck: boolean = false` to `RalphFeatures`. Write tests FIRST: deterministic checker pass/fail scenarios, LLM stub returns skip, PreCompact script generates valid JSON structure. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

### E — Loop Guardrails

- [x] **Atomic git commits per task**: Create `src/gitOps.ts` with `atomicCommit(workspaceRoot, task, taskInvocationId): Promise<CommitResult>` where `CommitResult = { success: boolean; commitHash?: string; error?: string }`. The function: (1) verifies committable state — no rebase/merge/cherry-pick in progress (checks `.git/rebase-merge`, `.git/MERGE_HEAD`, `.git/CHERRY_PICK_HEAD`), (2) runs `git add -A`, (3) generates conventional commit message — type `feat` or `fix` inferred from task description keywords, subject `{type}(task-{id}): {description truncated to 72 chars}`, body includes full description, `taskInvocationId` for traceability, changed files from `git diff --cached --name-only`, test results summary, (4) runs `git commit -m "{message}" --no-verify` (ralph's verification already ran), (5) captures commit hash from `git rev-parse HEAD`. Add `LoopEventKind.TaskCommitted = 'task_committed'` with `{ task, commitHash, taskInvocationId }`. In orchestrator, after TaskComplete hook, call `atomicCommit` — on success yield TaskCommitted and append to progress.txt; on failure yield Error but do NOT stop the loop. This is MANDATORY — every completed task produces a commit. Same in parallel task path. Write tests FIRST in `test/gitOps.test.ts`: commit message format, CommitResult with hash, error on rebase state, error on nothing to commit. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

- [x] **Stagnation detection via progress hashing**: Create `src/stagnationDetector.ts` exporting `StagnationDetector` class. Constructor accepts `hashFiles: string[]` (default `['progress.txt', 'PRD.md']`) and `maxStaleIterations: number` (default 2). Maintains `Map<string, string>` of previous file hashes and `staleCount`. Methods: (1) `snapshot(workspaceRoot)` — reads each file, computes SHA-256 hex digest (empty string hash if missing), stores in `currentHashes`; (2) `evaluate(): { stagnating: boolean; staleIterations: number; filesUnchanged: string[] }` — compares current vs previous hashes, increments `staleCount` if ALL unchanged otherwise resets to 0 (self-resetting counter), sets `stagnating = staleCount >= maxStaleIterations`, copies current to previous; (3) `reset()` — clears state. Add `LoopEventKind.StagnationDetected = 'stagnation_detected'` with `{ task, staleIterations, filesUnchanged }`. Add `stagnationDetection?: { enabled: boolean; maxStaleIterations: number; hashFiles: string[] }` to `RalphConfig` (default `{ enabled: true, maxStaleIterations: 2, hashFiles: ['progress.txt', 'PRD.md'] }`). In orchestrator, snapshot before prompt, evaluate after task attempt. Three-tier escalation: (1) at `maxStaleIterations` inject enhanced stagnation nudge as `additionalContext`: `"You appear to be stuck. Progress file has not changed. Try a different approach."`, (2) at +1 trigger circuit breaker, (3) at +2 yield HumanCheckpointRequested and break. Productive changes reset counter. Stagnation nudges separate from regular nudges. Write tests FIRST in `test/stagnationDetector.test.ts`: no stagnation when files change, stagnation after maxStaleIterations unchanged, counter resets on change, missing files don't crash, reset clears state. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass.

---

## Phase 6 — Knowledge & Resilience (from ecosystem analysis)

> Patterns extracted from: choo-choo-ralph (compounding knowledge), giocaizzi/ralph-copilot (auto-decomposition), aymenfurter/ralph (sanitization), hehamalainen/Ralph (security), vinitm/ralph-loop (error hashing), frankbria/ralph-claude-code (confidence scoring), rubenmarcus/ralph-starter (context trimming).
> **TDD is MANDATORY**: Every task MUST follow red-green-refactor. Write failing tests FIRST.
> Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> After completing each task: (1) run `npx tsc --noEmit` (must exit 0), (2) run `npx vitest run` (all tests pass), (3) append what you did to progress.txt, (4) mark the checkbox [x] in PRD.md, (5) commit with `git add -A && git commit -m "feat: <short description>"`.

### Baseline Fix (MANDATORY FIRST — blocks all other tasks)

- [x] **Fix all failing tests to establish green baseline**: There are 22 failing tests in `test/verify.test.ts` left over from Phase 5 work. The `VerifierRegistry`, `checkbox verifier`, `fileExists verifier`, `fileContains verifier`, `commandExitCode verifier`, `runVerifierChain`, `resolveVerifiers`, and `allChecksPassed` test suites are all failing. Root-cause the failures by reading `test/verify.test.ts` and `src/verify.ts`, identify the mismatch between test expectations and actual implementation (likely the Phase 5 refactor changed the `verify.ts` API surface but tests were written against a different shape), and fix ALL 22 tests. Do NOT delete or skip tests — fix the implementation or the test expectations to match. After fixing, run `npx tsc --noEmit` (must exit 0) and `npx vitest run` (ALL 172 tests must pass, 0 failures). This is a hard gate: no other Phase 6/7/8 task may be started until this baseline is green. Append what you did to progress.txt, mark this checkbox [x], and commit with `git add -A && git commit -m "fix: resolve 22 failing verify.test.ts tests to establish green baseline"`.

### Knowledge Persistence

- [x] **Compounding knowledge system**: Create `src/knowledge.ts` exporting `KnowledgeManager` class. Constructor accepts `knowledgePath: string` (default `'knowledge.md'`) and `maxInjectLines: number` (default 15). Methods: (1) `extractLearnings(output: string): string[]` — scan text for lines containing `[LEARNING]` tag (case-insensitive), return matched content stripped of the tag prefix; (2) `extractGaps(output: string): string[]` — scan for `[GAP]` tags, return content; (3) `persist(workspaceRoot, learnings: string[], gaps: string[])` — append timestamped entries to `knowledge.md` under `## Learnings` and `## Gaps` sections, create file with headers if missing; (4) `getRelevantLearnings(workspaceRoot, taskDescription: string): string[]` — read `knowledge.md`, extract all learnings, filter by keyword overlap (split task description into words ≥4 chars, match any learning containing ≥2 of those words), return up to `maxInjectLines` most recent matches. In `src/prompt.ts`, modify `buildPrompt` to accept optional `learnings?: string[]` parameter. When non-empty, insert a `PRIOR LEARNINGS` section after `AVAILABLE CAPABILITIES` containing the filtered learnings as bullet points. In `src/orchestrator.ts`, after each task completion, call `extractLearnings` and `extractGaps` on any captured Copilot output, then `persist`. Before building each task prompt, call `getRelevantLearnings` and pass results to `buildPrompt`. Add `knowledge?: { enabled: boolean; path: string; maxInjectLines: number }` to `RalphConfig` (default `{ enabled: true, path: 'knowledge.md', maxInjectLines: 15 }`). Write tests FIRST in `test/knowledge.test.ts`: extractLearnings finds tagged lines, extractGaps finds tagged lines, persist creates/appends file, getRelevantLearnings filters by keywords, empty input returns empty. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Stagnation Recovery

- [x] **3-fail auto-decomposition**: In `src/stagnationDetector.ts`, add `AutoDecomposer` class with method `shouldDecompose(taskId: string, failCount: number, threshold: number): boolean` that returns true when `failCount >= threshold` (default 3). Add `decomposeTask(task: PrdTask, prdContent: string): string` that takes the failed task and generates 2-3 sub-task lines formatted as PRD checkboxes (`- [ ] Sub-task: ...`) by splitting the task description at logical boundaries (sentence boundaries, semicolons, or numbered steps). Insert sub-tasks directly below the parent task line. Mark the parent with `[DECOMPOSED]` prefix: `- [ ] [DECOMPOSED] Original task...`. In `src/prd.ts`, modify `parseTasksFromPrd` to skip lines containing `[DECOMPOSED]` (treat as non-actionable). In `src/orchestrator.ts`, track consecutive verification failures per task. When `shouldDecompose` returns true, read PRD.md, call `decomposeTask`, write updated PRD.md, yield new `LoopEventKind.TaskDecomposed` event `{ originalTask, subTasks: string[] }`, and continue the loop (the decomposed sub-tasks will be picked up naturally by `pickNextTask`). Add `autoDecompose?: { enabled: boolean; failThreshold: number }` to `RalphConfig` (default `{ enabled: true, failThreshold: 3 }`). Write tests FIRST in `test/stagnationDetector.test.ts`: shouldDecompose returns false below threshold, true at threshold, decomposeTask generates valid checkbox lines, parseTasksFromPrd skips DECOMPOSED lines. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Security Hardening

- [x] **Input sanitization gate**: In `src/prompt.ts`, add `sanitizeTaskDescription(text: string): string` that: (1) strips all ASCII control characters (codes 0–31 except newline and tab) via regex `[\x00-\x08\x0B\x0C\x0E-\x1F]`, (2) escapes triple backticks by replacing ` ``` ` with ` \`\`\` ` to prevent code fence injection, (3) enforces a 5000-character limit by truncating with `... [truncated]` suffix, (4) strips any `<prompt>` or `</prompt>` XML-style tags that could be used for prompt injection. Call `sanitizeTaskDescription` on the task description inside `buildPrompt` before embedding it in the prompt output. Write tests FIRST in `test/copilot.test.ts` (or `test/prompt.test.ts`): control chars stripped, backticks escaped, long text truncated at 5000 chars, prompt injection tags removed, normal text unchanged. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

- [x] **Dangerous shell pattern pre-gate**: In `src/shellHookProvider.ts`, add a constant `DANGEROUS_PATTERNS = /&&|\|\||;|\||>|<|`|\$\(|\$\{/` (regex matching shell metacharacters: `&&`, `||`, `;`, `|`, `>`, `<`, backtick, `$()`, `${}`). Add `containsDangerousChars(cmd: string): boolean` that tests the command against this regex. In the existing command execution flow, call `containsDangerousChars` BEFORE any allowlist check — if dangerous chars are found, reject the command immediately with reason `"Blocked: shell metacharacters detected"`. This is defense-in-depth: the blocklist catches injection attempts before the allowlist even runs. Write tests FIRST in `test/hookBridge.test.ts` (or new `test/shellHook.test.ts`): `&&` blocked, `||` blocked, pipe blocked, redirect blocked, backtick blocked, `$()` blocked, clean commands pass through. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Smart Circuit Breaking

- [x] **Error hash deduplication in circuit breaker**: In `src/circuitBreaker.ts`, add `ErrorHashTracker` class. Methods: (1) `normalizeError(error: string): string` — strip timestamps (ISO 8601 regex), line numbers (`:123:` patterns), stack frame paths, ANSI escape codes, and collapse whitespace; (2) `hashError(error: string): string` — normalize then compute MD5 hex digest of the result (use Node.js `crypto.createHash('md5')`); (3) `record(error: string): { hash: string; count: number }` — hash the error, increment count in internal `Map<string, number>`, return hash and current count; (4) `isRepeating(hash: string, threshold: number): boolean` — return true when count for hash ≥ threshold (default 3); (5) `reset()` — clear the map. Add a new `RepeatedErrorBreaker` implementing `CircuitBreaker` interface: trips when `ErrorHashTracker.isRepeating` returns true for any error hash, action `'skip'`, reason includes the repeated error pattern (first 100 chars). In `createDefaultChain`, insert `RepeatedErrorBreaker` after `StagnationBreaker` (disabled by default). Add `repeatedErrorThreshold: number` (default 3) to circuit breaker config. In orchestrator, when a task errors, call `errorHashTracker.record(error.message)` and pass the tracker state to the circuit breaker chain. Write tests FIRST in `test/circuitBreaker.test.ts`: normalizeError strips timestamps and line numbers, identical errors produce same hash, different errors produce different hashes, isRepeating triggers at threshold, RepeatedErrorBreaker trips correctly. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Context Optimization

- [x] **Progressive context trimming**: In `src/prompt.ts`, modify `buildPrompt` to accept optional `iterationNumber: number = 1`. Implement 3-tier context reduction: (1) **Full** (iterations 1–3): current behavior — full progress tail, full PRD unchecked tasks, full learnings; (2) **Abbreviated** (iterations 4–8): reduce `maxProgressLines` to 10, reduce learnings to 8 lines, add `[context trimmed for iteration efficiency]` note; (3) **Minimal** (iterations 9+): reduce `maxProgressLines` to 5, no learnings injection, PRD shows only current task (not all unchecked), add `[minimal context mode — focus on current task only]` note. The tier boundaries are configurable via `contextTrimming?: { fullUntil: number; abbreviatedUntil: number }` in `RalphConfig` (default `{ fullUntil: 3, abbreviatedUntil: 8 }`). In `src/orchestrator.ts`, pass the current iteration number to `buildPrompt`. Write tests FIRST in `test/copilot.test.ts`: iteration 1 produces full context, iteration 5 produces abbreviated (shorter progress), iteration 10 produces minimal (only current task, no learnings), custom tier boundaries respected. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

---

## Phase 7 — Operational Excellence (from ecosystem analysis)

> Patterns extracted from: Th0rgal/opencode-ralph-wiggum (struggle detection, mid-loop injection), giocaizzi/ralph-copilot (structured reviews, task IDs), hehamalainen/Ralph (graceful kill, security feedback).
> **TDD is MANDATORY**: Every task MUST follow red-green-refactor. Write failing tests FIRST.
> Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> After completing each task: (1) run `npx tsc --noEmit` (must exit 0), (2) run `npx vitest run` (all tests pass), (3) append what you did to progress.txt, (4) mark the checkbox [x] in PRD.md, (5) commit with `git add -A && git commit -m "feat: <short description>"`.

### Human-in-the-Loop Steering

- [x] **Mid-loop context injection**: In `src/types.ts`, add `pendingContext?: string` to the orchestrator state and add `LoopEventKind.ContextInjected = 'context_injected'` event with `{ text: string }`. In `src/orchestrator.ts`, add `injectContext(text: string)` method that sets `pendingContext`. At the start of each iteration (before building the prompt), check for `pendingContext` — if set, pass it to `buildPrompt` as a new optional `operatorContext?: string` parameter, then clear `pendingContext` to null (consume-after-one-iteration semantics: context is used exactly once, not carried across iterations). In `src/prompt.ts`, when `operatorContext` is provided, insert an `OPERATOR CONTEXT (injected mid-loop)` section after `PRIOR LEARNINGS` containing the injected text. In `src/extension.ts`, register a VS Code command `ralph-loop.injectContext` that shows `vscode.window.showInputBox({ prompt: 'Enter context to inject into the next iteration', placeHolder: 'e.g., The bug is in utils/parser.ts line 42' })` and calls `orchestrator.injectContext(input)` with the result. Show info message confirming injection. Write tests FIRST in `test/orchestrator.test.ts`: injected context appears in prompt for one iteration then clears, null context produces no section, buildPrompt includes operator context section. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Quality Gates

- [x] **Structured review reports**: In `src/prompt.ts`, add `buildReviewPrompt(taskDescription: string, taskId: string): string` that generates a review prompt requesting this exact output format: `"## Review: Task-{id} — {title}\n**Verdict**: APPROVED | NEEDS-RETRY\n### Acceptance Criteria Check\n- [x/fail] Criterion — reason\n### Issues Found (if NEEDS-RETRY)\n1. **[Critical/Minor]**: issue with file:line\n### Fix Instructions\n1. Specific actionable fix"`. The prompt must instruct: `"You are a code reviewer. Verify, don't fix. Output ONLY the structured review format above. Check: correctness, code quality, test coverage, no dead code, no unused imports. Your verdict must be exactly APPROVED or NEEDS-RETRY."` In `src/copilot.ts`, add `parseReviewVerdict(reviewOutput: string): ReviewVerdict` that extracts the verdict by regex-matching `**Verdict**:\s*(APPROVED|NEEDS-RETRY)` (case-insensitive), extracts issues by matching numbered list items after "Issues Found", returns `{ outcome, summary, issues }`. Default to `'approved'` if parsing fails (fail-open for reviews). Update the existing `sendReviewPrompt` to use `buildReviewPrompt` and `parseReviewVerdict`. Write tests FIRST in `test/copilot.test.ts`: buildReviewPrompt contains format template, parseReviewVerdict extracts APPROVED, extracts NEEDS-RETRY with issues, defaults to approved on unparseable input. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Advanced Stagnation Detection

- [x] **3-signal struggle detection**: Create `src/struggleDetector.ts` exporting `StruggleDetector` class. Tracks 3 independent signals with consecutive-reset counters: (1) **No-progress signal**: after each iteration, check if any files changed using `git diff --name-only` (content-level, works without commits). Increment `noProgressCount` if zero changes, reset to 0 if any changes. Threshold: 3 consecutive. (2) **Short-iteration signal**: track iteration duration. Increment `shortIterationCount` if duration < 30000ms, reset to 0 on normal-length iteration. Threshold: 3 consecutive. (3) **Repeated-error signal**: reuse `ErrorHashTracker` from `circuitBreaker.ts`. After each iteration, extract errors from output via regex (`/error:|failed:|exception:|typeerror|syntaxerror/i`, line-by-line). If zero errors in an iteration, reset ALL error counts. Any error hash appearing ≥ 2x triggers. Methods: `recordIteration(duration: number, filesChanged: number, errors: string[]): void`, `isStruggling(): { struggling: boolean; signals: string[] }` (returns which signals are active), `reset(): void`. Add `LoopEventKind.StruggleDetected = 'struggle_detected'` event with `{ signals: string[], taskId }`. In orchestrator, call `recordIteration` after each task attempt. When `isStruggling()` returns true, inject guidance context: `"Struggle detected: {signals}. Try a completely different approach. If tests keep failing, check your assumptions."` and yield StruggleDetected. Add `struggleDetection?: { enabled: boolean; noProgressThreshold: number; shortIterationThreshold: number; shortIterationMs: number }` to `RalphConfig` (default `{ enabled: true, noProgressThreshold: 3, shortIterationThreshold: 3, shortIterationMs: 30000 }`). Write tests FIRST in `test/struggleDetector.test.ts`: no-progress counter increments and resets, short-iteration counter increments and resets, repeated errors trigger, single good iteration resets counters, isStruggling returns correct signals. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Traceability

- [x] **Task ID discipline**: In `src/prd.ts`, modify `parseTasksFromPrd` to assign sequential Task IDs (`Task-001`, `Task-002`, etc.) to each parsed task based on its order in the PRD file. Add `id: string` field to the `PrdTask` interface (e.g., `'Task-001'`). When generating the task ID, zero-pad to 3 digits. In `src/gitOps.ts`, modify `atomicCommit` to prefix the commit subject with the task ID: `"feat(Task-001): short description"`. In `src/orchestrator.ts`, include the task ID in all yielded events that reference a task. In `src/prompt.ts`, include the task ID in the `YOUR TASK TO IMPLEMENT` section header: `"YOUR TASK TO IMPLEMENT — Task-001"`. In progress.txt append format, prefix entries with the task ID: `"[Task-001] Completed: description..."`. Write tests FIRST in `test/prd.test.ts`: task IDs are sequential, zero-padded, consistent across parses. In `test/gitOps.test.ts`: commit message includes task ID prefix. In `test/copilot.test.ts`: prompt includes task ID in header. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Process Safety

- [x] **SIGTERM→SIGKILL graceful process kill**: In `src/shellHookProvider.ts`, add `killProcessTree(pid: number): void` that sends SIGTERM first, then after a 1-second delay sends SIGKILL. On Windows, use `taskkill /PID ${pid} /T /F` (tree kill with force). Wrap both signals in try-catch to handle "process already exited" ESRCH errors gracefully. Replace any existing `process.kill(pid, 'SIGKILL')` or direct kill calls with `killProcessTree`. Apply this to the hook script execution timeout handler — when a hook script exceeds the 30s timeout, use graceful kill instead of immediate SIGKILL. Write tests FIRST in `test/hookBridge.test.ts` (or `test/shellHook.test.ts`): SIGTERM sent first, SIGKILL sent after delay, ESRCH error handled, Windows path uses taskkill. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

- [x] **Security rejection as feedback**: In `src/shellHookProvider.ts`, when a command is blocked (by dangerous chars pre-gate or allowlist rejection), instead of silently skipping or hard-stopping, capture the rejection reason and feed it back to the orchestrator as `additionalContext` for the next prompt iteration. Add a return type to the command execution that includes `{ blocked: boolean; reason?: string }`. In `src/orchestrator.ts`, when a hook execution is blocked, inject the reason as context: `"Shell command blocked: {reason}. Provide a safe alternative that does not use shell metacharacters or chaining."` and continue the loop (do NOT stop). This lets the model self-correct on blocked commands. Add `LoopEventKind.CommandBlocked = 'command_blocked'` event with `{ command, reason, taskId }`. Write tests FIRST: blocked command produces feedback context, orchestrator continues after block, event is yielded. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

---

## Phase 8 — Advanced Patterns (from ecosystem analysis)

> Patterns extracted from: choo-choo-ralph (bearings phase), hehamalainen/Ralph (linked cancellation, model-agnostic), aymenfurter/ralph (prompt templates), frankbria/ralph-claude-code (confidence scoring, dual exit gate), Th0rgal/opencode-ralph-wiggum (session resume).
> **TDD is MANDATORY**: Every task MUST follow red-green-refactor. Write failing tests FIRST.
> Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> After completing each task: (1) run `npx tsc --noEmit` (must exit 0), (2) run `npx vitest run` (all tests pass), (3) append what you did to progress.txt, (4) mark the checkbox [x] in PRD.md, (5) commit with `git add -A && git commit -m "feat: <short description>"`.

### Pre-Flight Validation

- [x] **Bearings phase (pre-flight health check)**: In `src/orchestrator.ts`, add `runBearings(workspaceRoot: string, logger: Logger): Promise<BearingsResult>` where `BearingsResult = { healthy: boolean; issues: string[]; fixTask?: string }`. The function runs: (1) `npx tsc --noEmit` — if exit code ≠ 0, record TypeScript errors; (2) `npx vitest run` — if exit code ≠ 0, record test failures. If both pass, return `{ healthy: true, issues: [] }`. If either fails, return `{ healthy: false, issues: [...], fixTask: 'Fix baseline: {details}' }`. In the main loop, call `runBearings` BEFORE sending the task prompt. If unhealthy, insert a synthetic "fix baseline" task at the front of the queue — an auto-generated `- [ ] Fix baseline: resolve TypeScript errors and failing tests before continuing` line. The fix task gets one full iteration. If bearings still fail after the fix task, yield `LoopEventKind.BearingsFailed` event and pause for human intervention. Add `bearings?: { enabled: boolean; runTsc: boolean; runTests: boolean }` to `RalphConfig` (default `{ enabled: true, runTsc: true, runTests: true }`). Add `LoopEventKind.BearingsChecked = 'bearings_checked'` and `LoopEventKind.BearingsFailed = 'bearings_failed'` events. Write tests FIRST in `test/orchestrator.test.ts`: healthy bearings proceed to task, unhealthy inserts fix task, bearings disabled skips check. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Cancellation Architecture

- [x] **Linked CancellationToken pattern**: In `src/orchestrator.ts`, create `LinkedCancellationSource` utility class that combines multiple `AbortSignal` sources into one. Constructor accepts `...signals: AbortSignal[]`. Exposes `signal: AbortSignal` that aborts when ANY input signal aborts. Exposes `cancel(reason?: string)` for manual cancellation. Exposes `dispose()` to clean up listeners. In the orchestrator, create a linked source combining: (1) the manual stop signal (from `ralph-loop.stop` command), (2) a timeout signal (from `AbortSignal.timeout(inactivityTimeoutMs)`), (3) optionally a VS Code `CancellationToken` adapter (wraps `token.onCancellationRequested`). Replace the current multi-check approach (`if (stopRequested) ... if (timedOut) ...`) with a single `linkedSignal.aborted` check. Pass `linkedSignal.signal` down to all async operations (hook execution, shell commands, Copilot prompt sends). Write tests FIRST in `test/orchestrator.test.ts`: linked signal aborts when any source aborts, manual cancel works, dispose cleans up, multiple signals correctly linked. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Prompt Customization

- [x] **Custom prompt templates**: In `src/types.ts`, add `promptTemplate?: string` to `RalphConfig`. In `src/prompt.ts`, add `renderTemplate(template: string, variables: PromptVariables): string` where `PromptVariables = { task: string; prd: string; progress: string; learnings: string; workspace: string; taskId: string; iterationNumber: number }`. The function replaces `{{variable}}` placeholders with their values (simple string replacement, no eval). If `promptTemplate` is set in config, use `renderTemplate` instead of the built-in `buildPrompt` sections for the task-specific portion of the prompt. The built-in ROLE & BEHAVIOR, TDD GATE, and safety sections are ALWAYS prepended (non-overridable). A default template is provided: `"## Task: {{taskId}}\n{{task}}\n\n## Current PRD State\n{{prd}}\n\n## Recent Progress\n{{progress}}"`. In VS Code settings, expose `ralph-loop.promptTemplate` as a multiline string setting. Write tests FIRST in `test/copilot.test.ts`: template with all variables renders correctly, missing variables left as-is, built-in sections always present even with custom template, default template produces expected output. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Verification Enhancement

- [x] **Confidence-based completion scoring**: In `src/verify.ts`, add `computeConfidenceScore(checks: VerifyCheck[], diffResult?: DiffValidationResult): { score: number; breakdown: Record<string, number> }`. Scoring: checkbox checked = +100, tests pass (vitest check) = +20, TypeScript clean (tsc check) = +20, files changed (diff non-empty) = +20, no errors in output = +10, progress.txt updated (mtime check) = +10. Total possible = 180. Add `confidenceThreshold: number` (default 100) to `RalphConfig`. In orchestrator, after running the verifier chain, call `computeConfidenceScore`. If score < threshold, treat as incomplete — inject context `"Verification confidence: {score}/{max}. Missing: {failing checks}. Complete the remaining items."` and nudge. Yield `LoopEventKind.ConfidenceScored = 'confidence_scored'` event with `{ score, threshold, breakdown, taskId }`. A score ≥ threshold proceeds normally to TaskComplete. Write tests FIRST in `test/verify.test.ts`: all checks pass = max score, checkbox only = 100, no checks = 0, threshold comparison works, breakdown lists each component. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Crash Recovery

- [x] **Session resume on crash**: Create `src/sessionPersistence.ts` exporting `SessionPersistence` class. Methods: (1) `save(workspaceRoot, state: SerializedLoopState): void` — write JSON to `.ralph/session.json` containing `{ currentTaskIndex, iterationCount, nudgeCount, retryCount, circuitBreakerState, timestamp, version: 1 }`; (2) `load(workspaceRoot): SerializedLoopState | null` — read and parse `.ralph/session.json`, return null if missing or version mismatch; (3) `clear(workspaceRoot): void` — delete `.ralph/session.json`; (4) `hasIncompleteSession(workspaceRoot): boolean` — return true if file exists and timestamp is within 24 hours. In `src/orchestrator.ts`, call `save` after each iteration (after all events are yielded). Call `clear` when the loop completes normally or is stopped by user. In `src/extension.ts`, on activation, call `hasIncompleteSession` — if true, show `vscode.window.showInformationMessage('Ralph Loop has an incomplete session. Resume?', 'Resume', 'Discard')`. On Resume, call `load` and initialize the orchestrator with the saved state. On Discard, call `clear`. Add `sessionPersistence?: { enabled: boolean; expireAfterMs: number }` to `RalphConfig` (default `{ enabled: true, expireAfterMs: 86400000 }`). Write tests FIRST in `test/sessionPersistence.test.ts`: save creates file, load reads it back, clear deletes file, hasIncompleteSession returns true when fresh, false when expired, null on missing file. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

### Verification Architecture

- [x] **Dual exit gate**: In `src/verify.ts`, add `dualExitGateCheck(modelSignal: boolean, machineVerification: VerifyCheck[]): { canComplete: boolean; reason?: string }`. The function requires BOTH: (1) `modelSignal` is true (the model marked the checkbox — existing check), AND (2) ALL machine verifications passed (tsc, vitest, diff non-empty — the `allChecksPassed` function). Neither alone is sufficient. If model signals complete but machine checks fail, return `{ canComplete: false, reason: 'Model claims complete but verification failed: {details}' }`. If machine checks pass but model hasn't signaled, return `{ canComplete: false, reason: 'Verification passes but task not marked complete in PRD' }`. In orchestrator, replace the current sequential check (first checkbox, then verifiers) with `dualExitGateCheck` — only proceed to TaskComplete when both conditions are met simultaneously. This prevents false completions where the model marks done prematurely. Write tests FIRST in `test/verify.test.ts`: both pass = canComplete true, model only = false with reason, machine only = false with reason, neither = false. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

---

## Phase 9 — Adaptive Intelligence

> Specs: `research/14-phase9-refined-tasks.md` | Research: `research/13-phase9-deep-research.md`
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> After completing each task: (1) `npx tsc --noEmit` (exit 0), (2) `npx vitest run` (all pass), (3) append to progress.txt, (4) mark [x], (5) `git add -A && git commit -m "feat: <short description>"`.

### 9a — Context & Knowledge Intelligence

- [x] **Task 57 — Context Budget Awareness**: Add token budget estimation (`Math.ceil(len/3.5)`) with two configurable modes: `annotate` (inject budget % warning into prompt) and `handoff` (save state + start fresh chat at 90% threshold). → Spec: `research/14-phase9-refined-tasks.md` L15-L36
- [x] **Task 58 — Knowledge Harvest Pipeline**: Upgrade `KnowledgeManager` ingestion to composable pipeline: Extract→Dedup→Categorize→Score→Persist. Add `KnowledgeEntry` with hash-based dedup and keyword-based categorization. Each stage is a pure function, toggleable via config. → Spec: `research/14-phase9-refined-tasks.md` L40-L66
- [x] **Task 59 — Knowledge Garbage Collection**: Run-count based GC for `knowledge.md` — track retrieval hits per entry, archive stale entries (0 hits + age > threshold) to `knowledge-archive.md`, enforce `maxEntries` hard cap. Non-destructive (recoverable). → Spec: `research/14-phase9-refined-tasks.md` L70-L94

### 9b — Detection & Intelligence

- [x] **Task 60 — Thrashing Detection**: Add `ThrashingDetector` using region-hash fingerprinting from `git diff` hunk headers. Sliding window of edit hashes — same region 3+ times = thrashing. Integrates as 4th signal in `StruggleDetector`. → Spec: `research/14-phase9-refined-tasks.md` L100-L123
- [x] **Task 61 — Backpressure Classification**: Add `BackpressureClassifier` that interprets struggle signals as `productive` (errors decreasing), `stagnant` (errors flat, low unique ratio), or `thrashing` (delegates to ThrashingDetector). Determines orchestrator response per classification. → Spec: `research/14-phase9-refined-tasks.md` L127-L161
- [x] **Task 62 — Plan Regeneration**: Add `'regenerate'` circuit breaker action and `PlanRegenerationBreaker`. Triggers after decomposition fails — re-sends prompt with "take a different approach" context. Escalation chain becomes: nudge→decompose→regenerate→skip→stop. → Spec: `research/14-phase9-refined-tasks.md` L165-L192

### 9c — Prompt & Workflow

- [x] **Task 63 — Search-Before-Implement Gate**: In `src/prompt.ts`, after TDD GATE section, add unconditional `SEARCH-BEFORE-IMPLEMENT GATE` instructing the agent to search the codebase for existing implementations before writing new code. Prevents the #1 agent mistake: accidental duplication. Add test in `test/copilot.test.ts` verifying prompt contains the section. Run `npx tsc --noEmit` and `npx vitest run`.
- [x] **Task 64 — Workflow Presets**: Add `src/presets.ts` with named presets (general/feature/bugfix/refactor) as `Partial<RalphConfig>` overrides. Add `resolveConfig(preset, overrides)` merging DEFAULT_CONFIG←preset←user. Expose `ralph-loop.preset` VS Code setting. → Spec: `research/14-phase9-refined-tasks.md` L227-L284
- [x] **Task 65 — Inter-Task Cooldown Dialog**: During countdown between tasks, show auto-accepting dialog via `Promise.race` (VS Code has no auto-dismiss API). Buttons: Pause/Stop/Edit Next Task. Auto-continues after `countdownSeconds` timeout. → Spec: `research/14-phase9-refined-tasks.md` L288-L325

### 9d — Signals & Safety

- [x] **Task 66 — Filesystem Inactivity Signal**: Add `InactivityConfig` with configurable `timeoutMs` (default 120s, up from 60s concern), `warningAtPct` (50%), and `adaptive` flag (future). Graduated response: warning at 50%, action at 100%. → Spec: `research/14-phase9-refined-tasks.md` L331-L353
- [x] **Task 67 — Atomic Session Writes**: In `src/sessionPersistence.ts`, change `save()` to write to `.tmp` file then `fs.renameSync` to target — atomic on POSIX and Windows. Surgical 3-line fix, no API changes. Add test verifying crash safety. Run `npx tsc --noEmit` and `npx vitest run`.
- [x] **Task 68 — Session ID & Isolation**: Extend `SerializedLoopState` with `sessionId` (UUID), `pid`, `workspacePath`. Validate workspace match and PID liveness on `load()`. Prevents cross-window session interference. → Spec: `research/14-phase9-refined-tasks.md` L386-L412

### 9e — Research Infrastructure

- [x] **Task 69 — Frontmatter Parsing in buildPrompt**: Added `parseFrontmatter()`, `extractSpecReference()`, `buildSpecContextLine()` to `src/prompt.ts`. `buildPrompt()` now accepts `workspaceRoot` parameter and injects one-liner spec context from YAML frontmatter when task references a spec file. All 5 orchestrator call sites updated. 11 new tests added.
- [x] **Task 70 — Research File Frontmatter (13-14)**: Added YAML frontmatter to `research/13-phase9-deep-research.md` (type: research) and `research/14-phase9-refined-tasks.md` (type: spec). Frontmatter includes phase, sources, methodology, derived_specs, tasks, principles, verification, and completion_steps.
- [x] **Task 71 — Normalize Remaining Research Files**: Add YAML frontmatter to research files 01-12 using `/normalizeResearchFiles` prompt. Extract metadata from existing blockquote headers (`> Source:`, `> Date:`). Validate with `parseFrontmatter()`. Skip `README.md`, `_raw-*`, `_parsed-*`, and `AI_AGENT_ORCHESTRATION_COMPARISON.md`.

### 9f — External Signal Bridge

- [x] **Task 72 — ChatSendRequest Type**: Added `ChatSendRequest` interface to `src/types.ts` with `query?`, `mode?: 'agent'|'ask'|'edit'`, `isPartialQuery?`, and `sessionId?` fields. Extended `HookResult` with optional `chatSend?: ChatSendRequest`. Enables hooks to request mode switches or send messages to the chat panel programmatically.
- [x] **Task 73 — chatSend Command**: Added `ralph-loop.chatSend` command in `src/extension.ts`. Uses `workbench.action.chat.toggleAgentMode` with `modeId` (and optional `sessionResource` when `sessionId` provided) to switch chat modes. Optionally types and submits a query via `type` + `workbench.action.chat.submit`. Declared in `package.json` contributions.
- [x] **Task 74 — Signal File Watcher**: Added `startChatSendWatcher()` standalone function in `src/hookBridge.ts`. Watches `$TMPDIR/ralph-loop-chat-send.signal` via `fs.watchFile`. On content detection, parses JSON, fires `ralph-loop.chatSend`, then deletes the signal file. Wired into `activate()` in `src/extension.ts` and pushed to `context.subscriptions`. Enables external hooks (e.g., wave-orchestrator-stop.py) to trigger VS Code chat mode switches without VS Code API access.

### 9g — Verification Feedback Injection

- [x] **Task 75 — Verification Feedback Injection**: When verifiers fail, capture stderr/stdout diagnostic details in `VerifyCheck.detail` and inject structured failure reasons into the next prompt via `additionalContext` → `buildPrompt()`. Add `formatVerificationFeedback(checks: VerifyCheck[]): string` to `src/verify.ts`. Wire into orchestrator's verification-failed path so the agent receives actionable failure context (e.g., specific tsc errors, failing test names) instead of generic "verification failed" messages. Pattern from vercel-labs/Reflexion — highest-impact gap identified in wave research. → Spec: `research/_wave/2026-03-16-ralph-deep-verification/research-1.md`, `research/_wave/2026-03-16-ralph-deep-verification/research-7.md`

### 9h — Execution Robustness

- [x] **Task 76 — [CHECKPOINT] DSL Annotation**: Add `readonly checkpoint?: boolean` to `Task` interface in `src/types.ts`. In `parsePrd()` (`src/prd.ts`), detect `[CHECKPOINT]` tag in task descriptions — set `checkpoint: true` and strip the tag from the description. In the orchestrator (`src/orchestrator.ts`), add a checkpoint gate before agent execution: when `task.checkpoint === true`, immediately emit `LoopEventKind.HumanCheckpointRequested`, set `pauseRequested = true`, and auto-mark the task complete on resume via `markTaskComplete()`. Zero agent execution for checkpoint tasks — the human reviews progress and decides whether to continue. Add tests in `test/prd.test.ts`: checkpoint flag parsed correctly, annotation stripped from description, normal tasks unaffected, checkpoint not skipped like DECOMPOSED. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox. → Research: `research/_wave/2026-03-17-ralph-checkpoint-patterns/FINAL-REPORT.md`

- [x] **Task 77 — PRD Write Protection**: **NOTE: `validatePrdEdit` or equivalent may already be partially implemented — verify existing code in `src/prd.ts` and `src/orchestrator.ts` before starting; if already integrated, verify semantics match this spec and mark complete.** Create a ralph-loop agent mode (`.vscode/ralph-loop.agent.md` or equivalent restriction mechanism) that constrains what the executing agent can modify in `PRD.md`. Allowed mutations: (1) toggle checkbox `[ ]` → `[x]`, (2) prepend the DECOMPOSED prefix to a task description, (3) add/modify `depends:` annotations. All other edits to task descriptions, phase headers, or PRD structure must be rejected. This prevents the agent from weakening task acceptance criteria to make tasks easier to pass. Implementation: add `validatePrdEdit(before: string, after: string): { allowed: boolean; reason?: string }` to `src/prd.ts`. In the orchestrator's atomic commit path, read the PRD diff and reject commits where `validatePrdEdit` returns `allowed: false` — revert the PRD to its pre-edit state and inject a nudge warning. Write tests FIRST in `test/prd.test.ts`: checkbox toggle allowed, DECOMPOSED prefix allowed, depends annotation allowed, description text change rejected, phase header change rejected. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

- [x] **Task 78 — Dynamic Dependency Discovery (Tier 2.5)**: In the stagnation cascade (`src/orchestrator.ts`), between circuit breaker skip (Tier 2) and human checkpoint (Tier 3), add a dependency analysis step. When a task repeatedly fails (stagnation count ≥ 2 but below Tier 3 threshold), inject a prompt asking the agent: "Analyze WHY this task keeps failing. Identify if there is a prerequisite task that should have been completed first. If so, respond with `MISSING_DEP: <taskId>`." Parse the response — if `MISSING_DEP` is found and the referenced task exists and is incomplete, automatically add `depends: <taskId>` to the current task in `PRD.md` and move on to the dependency task first. This enables runtime dependency discovery without requiring upfront DAG planning. Add `analyzeMissingDependency(task: Task, failureContext: string): Promise<string | null>` to `src/prd.ts`. Wire into the stagnation handler in orchestrator. Write tests FIRST: dependency detected adds annotation, invalid taskId ignored, already-complete dependency ignored, no dependency returns null. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox. → Research: `research/_wave/2026-03-17-ralph-parallel-sequential/FINAL-REPORT.md`

- [x] **Task 79 — Sequential Mode Dependency Enforcement**: In `src/prd.ts`, change `pickNextTask()` to delegate to `pickReadyTasks(snapshot, 1)[0]` instead of `snapshot.tasks.find(t => t.status === TaskStatus.Pending)`. This makes sequential mode respect `dependsOn` annotations — a task with unmet dependencies is skipped in favor of the next ready task. Currently sequential mode ignores `dependsOn` entirely, which works by accident (PRD file order = execution order) but breaks when tasks declare explicit dependencies. Surgical 1-line fix. Write tests FIRST in `test/prd.test.ts`: task with unmet dep is skipped, task with met dep is picked, task with no deps behaves as before. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox. → Research: `research/_wave/2026-03-17-ralph-parallel-sequential/FINAL-REPORT.md`

### 9i — Bug Fixes

- [x] **Task 80 — Fix formatVerificationFeedback Export**: In `src/verify.ts`, restore the `export` on `formatVerificationFeedback(checks: VerifyCheck[]): string`. This function was implemented in Task 75 but its export was removed in a subsequent refactor (`b08b68c`), causing 8 test failures in `test/verify.test.ts` that import it. Surgical fix — add `export` keyword back to the function declaration. Run `npx tsc --noEmit` and `npx vitest run` — all 8 failing verify tests must now pass, bringing total from 462/470 to 470/470. No new tests needed — existing tests already cover this.

---

## Phase 10 — Agent Execution Model

> Currently ralph opens `workbench.action.chat.openEditSession` with whatever model/agent is active in VS Code — this is brittle and uncontrollable. Phase 10 introduces enforced agent modes, per-task agent selection, and tool restrictions so ralph controls exactly what the executing agent can and cannot do. Ralph already has `workbench.action.chat.toggleAgentMode` plumbing via the `ralph-loop.chatSend` command (Task 73) — Phase 10 wires this into the orchestrator loop so agent switching is deterministic.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
> After completing each task: (1) `npx tsc --noEmit` (exit 0), (2) `npx vitest run` (all pass), (3) append to progress.txt, (4) mark [x], (5) `git add -A && git commit -m "feat: <short description>"`.

### 10a — Default Agent Enforcement

- [x] **Task 81 — Ralph Agent Mode File**: Create `agents/ralph-executor.agent.md` (versioned in this repo) defining the default agent for ralph task execution. Symlink it into the global agents folder (`~/codes/llm-rules/vscode/Default/agents/ralph-executor.agent.md` — the path registered in `chat.agentFilesLocations` / `chat.modeFilesLocations` in `~/.config/Code - Insiders/User/settings.json`). Do NOT place it in `.github/agents/` or `.vscode/` locally — that would cause double-showing since the global folder already provides it to all workspaces. The agent file must: (1) set `model` to Claude Opus 4.6 fast mode (or equivalent `modelId` accepted by VS Code agent modes), (2) restrict tools to only those needed for implementation — file read/write, terminal, search, but NOT todo list management tools (ralph manages its own task state via PRD.md, the agent must not independently create/modify todo lists). Add `agentMode?: string` to `RalphConfig` in `src/types.ts` (default: `'ralph-executor'`). In `src/copilot.ts`, before calling `openEditSession`, call `vscode.commands.executeCommand('workbench.action.chat.toggleAgentMode', { modeId: config.agentMode })` to deterministically switch to the ralph agent mode — this uses the same `toggleAgentMode` API already wired in `ralph-loop.chatSend` (Task 73). Write tests in `test/copilot.test.ts`: agent mode switch command is called before openEditSession, default mode used when not overridden. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking this checkbox.

- [x] **Task 82 — Agent Tool Restrictions**: [DEFERRED — see #16] Original task incorrectly blocked `manage_todo_list` from `ralph-executor`, but executor is the top-level agent (not a leaf node) and needs full tool access. Upstream vscode-copilot-chat confirms: parent agent has full tools, only `SearchSubagentToolCallingLoop` restricts to a 4-tool allowlist. Leaf agents (`ralph-explore`, `ralph-research`) already have correct restrictions from Task 84. The `blockedTools` config and prompt injection infrastructure deferred until subagent dispatch is implemented. Fixed executor agent file: restored `manage_todo_list` and `memory` tools, removed incorrect TOOL RESTRICTIONS section.

### 10b — Per-Task Agent Annotation

- [x] **Task 83 — Agent DSL Annotation**: Add agent annotation to the PRD DSL in `src/prd.ts`, similar to checkpoint annotations. When `parsePrd()` encounters an AGENT annotation in a task description, set `agent?: string` on the `Task` interface and strip the annotation from the description. Add `readonly agent?: string` to `Task` in `src/types.ts`. Built-in agent names: `implement` (default — full write access), `explore` (read-only — search/read tools only, no file writes or terminal), `research` (read-only + web search tools). If no agent annotation, tasks use the default `ralph-executor` agent from Task 81. Write tests in `test/prd.test.ts`: annotation parsed and stripped, agent name stored on task, missing annotation defaults to undefined, multiple annotations in same task rejected. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 84 — Agent-Routed Execution**: In `src/orchestrator.ts`, modify the task execution path to select the agent based on `task.agent`. Instead of always calling `sendToCopilot()` with the default agent mode, resolve the agent mode name: (1) if `task.agent` is set, use `ralph-{task.agent}` as the mode ID, (2) otherwise use the default from `config.agentMode`. In `src/copilot.ts`, extend `sendToCopilot()` to accept an `agentMode` parameter that overrides the config default. Each agent mode defines its own model and tool restrictions. Create `agents/ralph-explore.agent.md` (read-only tools, same model) and `agents/ralph-research.agent.md` (read-only + web search) — versioned in this repo, symlinked into `~/codes/llm-rules/vscode/Default/agents/` (same pattern as Task 81). Write tests: correct agent selected per annotation, fallback to default when no annotation, unknown agent name falls back to default with warning. Run `npx tsc --noEmit` and `npx vitest run`.

### 10c — Safe Execution Strategies

- [x] **Task 85 — Read-Only Agent Detection**: Add `isReadOnlyAgent(agentName: string): boolean` to `src/prd.ts` that returns `true` for agents with no write tools (`explore`, `research`). In the orchestrator, when picking ready tasks for parallel execution (`pickReadyTasks`), allow parallel batches ONLY if ALL tasks in the batch use read-only agents. If any task in the batch uses a write agent, fall back to sequential execution for that batch. This makes parallel execution safe for exploration/research phases while preventing git conflicts during implementation. Add `readonly readOnly?: boolean` to the agent metadata. Write tests: explore agent detected as read-only, implement agent not read-only, mixed batch falls back to sequential, all-explore batch runs parallel. Run `npx tsc --noEmit` and `npx vitest run`. → Research: `research/_wave/2026-03-17-ralph-parallel-sequential/FINAL-REPORT.md`

- [x] **Task 86 — Phase-Level Agent Override**: Add agent annotation support at the phase header level (e.g., `### 9e — Research Infrastructure` with an agent tag). When a phase header has an agent annotation, ALL tasks in that phase inherit it unless individually overridden. In `parsePrd()`, track the current phase's agent annotation and apply it as default for tasks within that phase. Task-level agent annotation takes precedence over phase-level. Write tests in `test/prd.test.ts`: phase agent inherited by tasks, task-level overrides phase-level, no phase agent leaves task default, phase agent stripped from header text. Run `npx tsc --noEmit` and `npx vitest run`.

### 10d — Known Issues

- [x] **Task 87 — File Parallel Mode Bug Issues**: Create GitHub issues in the ralph-loop bug tracker for the known parallel mode defects identified in wave research. Issues to create: (1) **Git race condition** — `atomicCommit()` uses `git add -A` which stages all files, causing parallel tasks to commit each other's changes or conflict; fix: scope `git add` to task-specific files. (2) **No cycle detection** — `pickReadyTasks()` does not detect circular `dependsOn` references, causing infinite loops where no task is ever "ready"; fix: add cycle detection in `parsePrd()` or `pickReadyTasks()`. (3) **False AllDone bug** — when all remaining tasks have unmet dependencies, orchestrator emits `AllDone` instead of detecting the deadlock; fix: distinguish "no pending tasks" from "pending but blocked". (4) **Fallback breaks dependency contract** — parallel mode falls back to `pickNextTask()` when `pickReadyTasks()` returns empty, bypassing dependency enforcement entirely. Each issue should reference `research/_wave/2026-03-17-ralph-parallel-sequential/FINAL-REPORT.md` as source. Run `npx tsc --noEmit` and `npx vitest run` (no code changes, just issue creation). → Research: `research/_wave/2026-03-17-ralph-parallel-sequential/FINAL-REPORT.md`

---

## Phase 11 — Wave Pipeline Integration & Verification

> Verifies, integrates, and hardens the wave pipeline components built in `vscode-config-files`. Each task follows a verify-first pattern: confirm the upstream implementation exists and is correct, integrate it into ralph-loop if needed, fix any gaps, then mark complete. This ensures no stale checkboxes — ralph rigorously validates every component.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.
>
> **⚠️ WORKING DIRECTORY**: `ralph-loop` is the working repo for ALL tasks in this phase. Your `cwd` must remain `ralph-loop/` at all times. When tasks reference `vscode-config-files/` paths (agents, prompts), those are **sibling repo paths you read/verify from ralph-loop** — you do NOT `cd` into vscode-config-files or treat it as the active project. When a gap fix requires editing a file in `vscode-config-files/agents.source/`, use the relative path `../vscode-config-files/agents.source/` from ralph-loop or the absolute path — never change your working directory.

### 11a — Patterns & Documentation (verified)

- [x] **Task 24 — Frontmatter Sealing Pattern**: Document the "frontmatter as sealed source of truth" pattern in `docs/patterns/frontmatter-sealing.md`. Key constraint: frontmatter (tasks, verification, completion_steps, principles) must be the **last transformation** applied to spec files — after all research, synthesis, and user refinement is complete. Ralph's `buildPrompt()` reads frontmatter to drive execution; applying it mid-pipeline with incomplete data causes partial specs. Sequence: `Research → Spec (raw, no frontmatter) → User refines → Seal (apply frontmatter) → PRD entries with → Spec: pointers`. Include examples from existing research files.

### 11b — Agent Verification & Integration

- [x] **Task 25 — Verify & Integrate Context Grounder Agent**: Verify that `vscode-config-files/agents.source/wave-context-grounder.agent.md` exists and contains: (1) PRD.md + README.md reading instructions, (2) ContextBrief output format (≤2K tokens, ≤30 lines) that captures: what the project already has (prevents researching solved problems), naming conventions, current architecture, existing phase/task numbering, (3) instruction that the ContextBrief gets injected into all subsequent subagent prompts as grounding context, (4) 3-zone commit-sampling section (from Task 32), (5) `contextSources` composable chain (from Task 34). **Verification**: run `grep -c '3-zone\|commit.sampling\|ContextBrief\|contextSources' vscode-config-files/agents.source/wave-context-grounder.agent.md` — expect ≥4 matches. **Integration**: the canonical file stays in `vscode-config-files/agents.source/wave-context-grounder.agent.md` (centralized agent repo). In `ralph-loop/agents/`, create a symlink: `ln -sf ../../vscode-config-files/agents.source/wave-context-grounder.agent.md agents/wave-context-grounder.agent.md`. Verify the symlink resolves from ralph-loop's working directory. Confirm ralph-loop's AGENTS.md references `wave-context-grounder` in the agents list with correct description. **Note**: all edits to the agent file must be made at the canonical location (use path `../vscode-config-files/agents.source/wave-context-grounder.agent.md` from ralph-loop's cwd) — ralph-loop only consumes it via symlink. **Gap fix**: if any feature is missing or incomplete, edit the canonical file via `../vscode-config-files/agents.source/wave-context-grounder.agent.md` — do NOT cd into vscode-config-files.

- [x] **Task 26 — Verify & Integrate Spec Generator Agent**: Verify that `vscode-config-files/agents.source/wave-spec-generator.agent.md` exists and contains: (1) FINAL-REPORT.md + ContextBrief input spec, (2) output produces raw task specs (interfaces, config, tests) as markdown, (3) auto-numbering logic for phase/task/file numbers — phase from `## Phase N` headers (`max(N) + 1`), task from `- [ ] **Task NN` patterns (`max(NN) + 1`), research file from `research/NN-*.md` filenames (`max(NN) + 1`), (4) output format `research/{NN}-phase{P}-deep-research.md` (raw, NO frontmatter — see Task 24), (5) this is the refactored core of `/researchPhase` Waves 3-4 minus the fan-out/synthesis (which wave-orchestrator handles). **Verification**: read the agent file and confirm all sections are present with concrete instructions (not placeholders). **Integration**: the canonical file stays in `vscode-config-files/agents.source/wave-spec-generator.agent.md` (centralized agent repo). In `ralph-loop/agents/`, create a symlink: `ln -sf ../../vscode-config-files/agents.source/wave-spec-generator.agent.md agents/wave-spec-generator.agent.md`. Verify the symlink resolves from ralph-loop's working directory. **Note**: all edits to the agent file go to the canonical location via `../vscode-config-files/agents.source/wave-spec-generator.agent.md` from ralph-loop's cwd — do NOT cd into vscode-config-files. **Gap fix**: if auto-numbering logic is vague or missing regex patterns, add explicit examples. If the output path format is inconsistent with existing research files, align it.

- [x] **Task 27 — Verify & Integrate PRD Generator Agent**: Verify that `vscode-config-files/agents.source/wave-prd-generator.agent.md` exists and contains: (1) sealed spec file reading (with frontmatter), (2) Tier 1 vs Tier 2 task classification logic, (3) `→ Spec:` line-range pointer generation, (4) phase section header generation (`## Phase N — Name`), (5) task entries in ralph-loop PRD format (`- [ ] **Task NN — Name**: Description`), (6) output is presented to user for review before writing to PRD.md. **Verification**: read the agent file and confirm tier classification rules are concrete (not just "classify tasks"). Verify the output format matches the actual PRD.md format used in this file (check against any existing task entry). Verify user-review gate is documented. **Integration**: the canonical file stays in `vscode-config-files/agents.source/wave-prd-generator.agent.md` (centralized agent repo). In `ralph-loop/agents/`, create a symlink: `ln -sf ../../vscode-config-files/agents.source/wave-prd-generator.agent.md agents/wave-prd-generator.agent.md`. Verify the symlink resolves from ralph-loop's working directory. **Note**: all edits to the agent file go to the canonical location via `../vscode-config-files/agents.source/wave-prd-generator.agent.md` from ralph-loop's cwd — do NOT cd into vscode-config-files. **Gap fix**: if line-range indexing is ambiguous, add an explicit `Build Line Range Index` section. If the output doesn't match ralph-loop's actual PRD format, fix it. If user-review gate is missing, add it.

### 11c — Pipeline Verification & Wiring

- [x] **Task 28 — Verify researchPhase Prompt Refactoring**: Verify that `../vscode-config-files/prompts/researchPhase.prompt.md` (read from ralph-loop cwd) has been refactored to delegate fan-out/synthesis to wave-orchestrator — wave-orchestrator handles these better with parallel subagents. The prompt should now focus on spec transformation only. **Verification**: read the prompt file — it should NOT contain Wave 1 (fan-out) or Wave 2 (synthesis) logic, only spec transformation (Wave 3: transform findings → task specs) and frontmatter application (Wave 4). Check that references to the old `/researchPhase` flow in other prompts have been updated. **Gap fix**: if old fan-out logic remains, strip it. If other prompts still reference the old flow, update the references. Depends on Task 26 (spec generator agent) existing as the replacement for the stripped logic.

- [x] **Task 29 — Verify updatePRD Prompt Handoff**: Verify that `../vscode-config-files/prompts/updatePRD.prompt.md` (read from ralph-loop cwd) accepts a spec file path as input for pipeline invocation. Currently requires manual spec file selection; this handoff entry point makes it callable as a pipeline step from `--ralph-prd`. **Verification**: read the prompt file — it should have a handoff entry point section that accepts a file path argument (not just manual spec selection). **Gap fix**: if the handoff entry point is missing, add a section that accepts `{SPEC_FILE_PATH}` as an argument and uses it to read the spec file directly.

- [x] **Task 30 — Verify wave-orchestrator --ralph-prd Mode**: Verify that `../vscode-config-files/agents.source/wave-orchestrator.agent.md` (read from ralph-loop cwd) contains a complete `--ralph-prd` mode section. **Verification**: run `grep -c 'ralph-prd' ../vscode-config-files/agents.source/wave-orchestrator.agent.md` — expect ≥3 matches. Read the section and confirm it defines all 6 phases (Context → Research → Spec → Seal → PRD → Finalize) with 3 human checkpoints (after Research, after Spec, after PRD). Verify each checkpoint lists the user options (Continue/Refine/Back/Stop). **Integration**: the wave-orchestrator agent is shared infrastructure used by all workspaces — keep canonical file in `vscode-config-files/agents.source/` (it's not ralph-loop-specific). If ralph-loop needs direct access, create symlink from ralph-loop cwd: `ln -sf ../../vscode-config-files/agents.source/wave-orchestrator.agent.md agents/wave-orchestrator.agent.md`. **Gap fix**: if any phase is missing, incomplete, or lacks checkpoint options, add it. If the phase numbering doesn't match the diagram (Phase 0-5), fix it.

- [x] **Task 31 — Verify wave-explore-fast --ralph-prd Entry Point**: Verify that `../vscode-config-files/prompts/wave-explore-fast.prompt.md` (read from ralph-loop cwd) includes `--ralph-prd` in its argument-hint and delegates to wave-orchestrator's `--ralph-prd` mode. **Verification**: run `grep -c 'ralph-prd' ../vscode-config-files/prompts/wave-explore-fast.prompt.md` — expect ≥2 matches. Read the file and confirm the delegation logic is explicit (not just a mention). **Gap fix**: if the flag is mentioned but delegation is vague, make it explicit: "When `--ralph-prd` is present, delegate to wave-orchestrator `--ralph-prd` mode instead of the standard explore flow." Depends on Task 30.

### 11d — Advanced Features Verification

- [x] **Task 32 — Verify Commit-Sampling in Context Grounder**: Verify that wave-context-grounder agent includes 3-zone git log analysis: first 10 commits (founding intentions) + middle 10 (evolutionary trajectory) + last 10 (current state). Expected commands: `git log --oneline` + `git show --stat` → LLM summarizes into codebase fingerprint (top files by churn, key function signatures, dependency list). Adds ~30s latency — consider pre-computed cache (see Task 35). **Verification**: read `../vscode-config-files/agents.source/wave-context-grounder.agent.md` (from ralph-loop cwd) and confirm the commit-sampling section contains explicit `git log` commands, the 3-zone sampling strategy, and the LLM summarization output format. **Gap fix**: if the section exists but lacks concrete git commands or the codebase fingerprint output spec, add them. If latency mitigation (cache reference to Task 35) is missing, add a note. Depends on Task 25.

- [x] **Task 33 — Verify Checkpoint Retry Pattern**: Verify that (1) `docs/patterns/checkpoint-retry.md` exists in ralph-loop with the file-state go-back pattern, and (2) it defines `phase-{N}-state.json` schema (inputs, outputs, user steering, timestamp). This pattern generalizes the existing ralph-loop `sessionPersistence.ts` pattern to the wave pipeline. **Verification**: read the pattern doc and confirm it covers: state file location (`research/_wave/{WAVE_ID}/`), go-back mechanics ("go back" = re-run phase N subagent with same inputs + appended user feedback), and the relationship to `sessionPersistence.ts`. **Gap fix**: if the pattern doc is incomplete or the state schema is missing, add it. If `src/checkpointRetry.ts` doesn't exist yet, note it as a future implementation task. Depends on Task 30.

- [x] **Task 34 — Verify Context Source Chain**: Verify that wave-context-grounder agent supports composable context sources: `contextSources: [prd, commits-3zone, readme, changelog]`. Each source should be a pure function `(workspace) → ContextSnippet`. The chain concatenates and trims to a token budget. Users can add custom sources (e.g., `architecture.md`) or disable any. **Verification**: read the agent file and confirm the chain is documented with: source list, pure function signature, token budget trimming, custom source extensibility, and ability to disable sources. **Gap fix**: if the chain is described but lacks the pure function design or concrete examples of adding/disabling custom sources, add them. If it's missing entirely, add the composable sources section. Depends on Tasks 25 and 32.

- [x] **Task 35 — Verify Codebase Brief Cache**: Verify that wave-context-grounder agent supports `.ralph/codebase-brief.md` as a pre-computed cache, regenerated on significant changes via git hook or periodic script. Makes Phase 0 near-instant instead of requiring LLM summarization each run. **Verification**: read the agent file — it should describe: (1) cache file location (`.ralph/codebase-brief.md`), (2) regeneration trigger (git hook or periodic script), (3) cache invalidation when `git diff --stat HEAD~5` shows significant changes, (4) fallback to live summarization when cache is stale. **Gap fix**: if the cache concept is referenced but invalidation logic or regeneration trigger is missing, add it. If the `.ralph/` directory convention isn't documented, add a note. Depends on Task 32.

### 11e — E2E Pipeline Validation & Remaining Gaps

- [x] **Task 89 — [CHECKPOINT] Wave Pipeline Integration Review**: Human checkpoint. Review all Tasks 25-35 verification results. Confirm: (1) ALL agent files live canonically in `vscode-config-files/agents.source/` (single centralized location — no scattering), (2) `ralph-loop/agents/` contains only symlinks pointing to `../../vscode-config-files/agents.source/` — no regular file copies, (3) all symlinks resolve correctly: `file ralph-loop/agents/*.agent.md` should show all entries as symbolic links, (4) all prompts in `vscode-config-files/prompts/` are wired properly, (5) `docs/patterns/` has both `frontmatter-sealing.md` and `checkpoint-retry.md`, (6) ralph-loop's AGENTS.md lists all wave agents with correct descriptions. If any gaps remain, create follow-up tasks before proceeding.

- [x] **Task 89.1 — Remove "Wave Pipeline Agents" section from AGENTS.source.md**: Agents are first-class citizens auto-discovered from `agents/` directory — they do NOT need manual listing in AGENTS.md. Remove the entire `## Wave Pipeline Agents` section from `vscode-config-files/AGENTS.source.md` (including the `wave-context-grounder` entry added by Task 25). Also grep AGENTS.source.md for any other stray agent file references (`.agent.md`) outside the skills/agents system sections and remove them. The `agents:` list in each `.agent.md` frontmatter is what defines available subagents — AGENTS.md is for skills discovery only.

- [x] **Task 90 — E2E Pipeline Smoke Test**: Run the `--ralph-prd` pipeline end-to-end on a small test topic to validate the full flow works. Steps: (1) invoke `wave-explore-fast.prompt.md` with `3 --ralph-prd test authentication improvements`, (2) verify Phase 0 produces a ContextBrief, (3) verify Phase 1 produces research reports + FINAL-REPORT.md, (4) verify Phase 2 produces a spec file, (5) verify Phase 3 seals the spec with frontmatter, (6) verify Phase 4 produces PRD entries with correct format. Document any failures and create fix tasks. This is validation only — do not merge the test output into the real PRD.

- [x] **Task 91 — Reference Chain Validation Tooling**: Add a verification script or function that validates `→ Spec:` line-range pointers in PRD.md are not stale. For each `→ Spec: research/{file}` pointer, confirm: (1) the referenced file exists, (2) if line ranges are specified (e.g., `L10-L50`), the content at those lines matches expectations (not shifted by edits). Report stale pointers. This prevents silent breakage when spec files are edited after PRD entries are written. Implementation: add `validateSpecPointers(prdContent: string, researchDir: string): StalePointer[]` to `src/prd.ts`. Write tests FIRST: valid pointer passes, missing file detected, shifted line range detected. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 92 — Agent Sync via Symlinks (vscode-config-files = centralized source of truth)**: **Decision**: `vscode-config-files/agents.source/` is the single canonical home for ALL agent files — ralph-specific, wave-specific, and shared infrastructure. No agent files scattered across repos. `ralph-loop/agents/` contains ONLY symlinks. **Working repo is ralph-loop** — all task execution happens here, but agent edits go to `../vscode-config-files/agents.source/` (via relative path from ralph-loop cwd — never cd into vscode-config-files). Steps: (1) For each agent currently existing as a regular file in `ralph-loop/agents/` (`ralph-executor.agent.md`, `ralph-explore.agent.md`, `ralph-research.agent.md`), delete the regular file and replace with a symlink: `ln -sf ../../vscode-config-files/agents.source/{file} ralph-loop/agents/{file}`. (2) For wave agents already symlinked by earlier tasks (context-grounder, spec-generator, prd-generator from Tasks 25-27), verify symlinks are correct. (3) For any remaining agents in `vscode-config-files/agents.source/` that ralph-loop needs (wave-orchestrator, wave-decompose, etc.), create symlinks in `ralph-loop/agents/`. (4) Validate: `file ralph-loop/agents/*.agent.md` — ALL entries must show as symbolic links, zero regular files. (5) Validate: all symlinks resolve correctly: `ls -la ralph-loop/agents/ | grep '\->' | wc -l` should equal total agent count. (6) Add a validation script `scripts/check-agent-sync.sh` that detects broken symlinks. (7) Document this centralized convention in `docs/conventions/agent-ownership.md`.

---

## Phase 12 — Cross-Extension State Notification

> Best practice for VS Code extension-to-extension state sync is **event-driven push** via fired commands rather than polling. Currently the copilot-chat fork polls `ralph-loop.status` and `ralph-loop.taskName` every 5 seconds — this is laggy (up to 5s stale) and wastes CPU cycles. Phase 12 replaces polling with a push model where ralph-loop fires a notification command on every state transition, which the copilot-chat fork listens to for instant status bar updates.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.

### 12a — Push-Based State Notification

- [x] **Task 93 — State Change Notification Command**: In `src/extension.ts`, after each state transition in the orchestrator event handler (TaskStarted, TaskCompleted, Paused, Stopped, Resumed, etc.), fire `vscode.commands.executeCommand('ralph-loop.onStateChange', { state, taskId })` where `state` is the current `LoopState` string and `taskId` is the current task's ID (or empty string if idle). This is a fire-and-forget notification — the command may not be registered (copilot fork not installed) and that's fine (`executeCommand` returns a rejected promise that we catch and ignore). Add `LoopEventKind.StateNotified` for telemetry. In the orchestrator, yield a `StateNotified` event after each state transition. Write tests: notification fired on TaskStarted, fired on TaskCompleted, fired on stop, taskId is empty when idle. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 94 — Expose State Snapshot Command**: Add `ralph-loop.getStateSnapshot` command in `src/extension.ts` that returns `{ state: string, taskId: string, taskDescription: string, iterationCount: number, nudgeCount: number }` — a richer payload than the existing `ralph-loop.status` (which returns only the state string) and `ralph-loop.taskName` (which returns only the task ID). This gives consumers a single call to get all ralph-loop state. The existing `ralph-loop.status` and `ralph-loop.taskName` commands remain for backward compatibility. Run `npx tsc --noEmit` and `npx vitest run`.

---

## Phase 13 — Copilot Chat Fork Status Bar Enhancements

> Enhancements to the copilot-chat fork's status bar (`vscode-copilot-chat/src/extension/prompt/node/ralphStatusBar.ts`). These tasks modify fork code, not ralph-loop code, but are tracked here since they are part of the ralph-loop project scope.
> **Before implementing any task, verify the current code state** — read `ralphStatusBar.ts` and `chatParticipantRequestHandler.ts` to confirm what is/isn't already implemented.

### Current Code State (as-is, verified 2026-03-18)

**What IS implemented** in `ralphStatusBar.ts`:
- `loopTag()` function with icon map: `running → $(sync~spin)`, `paused → $(debug-pause)`, `idle → $(circle-outline)`
- `pollRalphLoop()` polls every 5 seconds via `ralph-loop.status` and `ralph-loop.taskName`
- `loopState` is set to empty string when state is `'idle'` (`state && state !== 'idle' ? state : ''`), so the idle icon is **never rendered** — `loopTag()` returns `''` when `loopState` is empty
- Task name is only fetched when `loopState` is non-empty (i.e., running or paused)
- `ralphStatusActive(modelName, multiplier, billingTag)` — no `modelFamily` parameter
- `ralphStatusIdle(modelName, multiplier, billingTag, prTag, duration)` — no `modelFamily` parameter
- PR counter in both active and idle states
- `↑`/`—` indicator only in idle state

**What is NOT implemented**:
- Idle icon never displays (filtered out before `loopTag()` is called)
- No effort tag (`E:high` etc.) — no model family detection, no setting reads
- No click-to-change reasoning effort
- No tooltip enhancement
- No push-based state updates (depends on Phase 12 Tasks 93-94)

### 13a — Ralph Status Icon Fixes

> The idle icon `$(circle-outline)` is defined in `loopTag()` but never rendered because `pollRalphLoop()` sets `loopState = ''` when state is `'idle'`.

- [x] **Task 95 — Show idle state icon**: In `pollRalphLoop()`, change the idle filtering logic from `loopState = state && state !== 'idle' ? state : ''` to `loopState = state || ''`. This makes `loopTag()` render `Ralph: $(circle-outline)` when ralph-loop is idle. Also fetch `loopTaskName` regardless of state (not only when running/paused) so the last task ID is shown in idle too. In `loopTag()`, change the separator between icon and taskName from ` • ` (bullet) to ` ` (space) so the format matches the agreed visual: `Ralph: $(sync~spin) T3` not `Ralph: $(sync~spin) • T3`. **Verification**: with ralph-loop installed and idle, status bar should show `… • Ralph: $(circle-outline) T3` (last completed task ID). With ralph-loop running, should show `… • Ralph: $(sync~spin) T3`. With ralph-loop not installed, no Ralph section.

### 13b — Reasoning Effort Display & Toggle

> Show the current reasoning effort setting next to the model name in the status bar, with a click-to-change quick pick dropdown. Provider-aware: Anthropic models show `anthropic.thinking.effort`, OpenAI models show `responsesApiReasoningEffort`.

- [x] **Task 96 — Effort tag display**: Add model family detection (`resolveModelFamily(family: string): 'anthropic' | 'openai' | 'other'`) to `ralphStatusBar.ts`. Detection logic: family string starting with `claude` or `Anthropic` → `'anthropic'`, starting with `gpt`/`o1`/`o3`/`o4` → `'openai'`, otherwise → `'other'`. Add `modelFamily` parameter to both `ralphStatusActive(modelName, multiplier, billingTag, modelFamily)` and `ralphStatusIdle(modelName, multiplier, billingTag, prTag, duration, modelFamily)`. Read the relevant setting on each status bar update: Anthropic reads `github.copilot.chat.anthropic.thinking.effort` (enum values: `low`, `medium`, `high`; default: `high`), OpenAI reads `github.copilot.chat.responsesApiReasoningEffort` (enum values: `low`, `medium`, `high`, `xhigh`, `default`; default: `default`). Build effort tag: `effortTag(family) → ' • E:{value}'` for anthropic/openai, `''` for other. Insert between billing/PR counter and Ralph section. In `chatParticipantRequestHandler.ts`, pass `endpoint.family` as the new `modelFamily` argument to both `ralphStatusActive` and `ralphStatusIdle` (lines ~250 and ~271). Settings are read synchronously via `workspace.getConfiguration().get()` — no listener needed since the tag is re-read on every status bar update.

- [x] **Task 97 — Effort quick pick toggle**: Register command `copilot.ralph.cycleEffort` that shows a `window.showQuickPick` with the allowed values for the current model family. Current value marked with `$(check)` icon or `(current)` description. On selection, update the setting via `workspace.getConfiguration('github.copilot').update(key, value, ConfigurationTarget.Global)`. For `'other'` model families, show info message "No reasoning effort setting for this model". Register the command lazily in `ensureItem()` (guarded by a boolean flag) to avoid wiring into extension activation. Set `item.command = 'copilot.ralph.cycleEffort'` on the status bar item.

- [x] **Task 98 — Tooltip enhancement**: Set `item.tooltip` to show a summary of the current state: model name, billing mode, PR count, effort level, ralph-loop status. Use a multi-line `MarkdownString` for rich formatting. Include "Click to change reasoning effort" guidance.

### 13c — Push-Based Ralph State Updates

> Depends on Phase 12 (Tasks 93-94). Replace 5-second polling with instant push notifications.

- [x] **Task 99 — Register notification listener**: Register a `ralph-loop.onStateChange` command handler in `ralphStatusBar.ts` that receives `{ state, taskId }` payload and immediately updates `loopState` and `loopTaskName`, then refreshes the status bar text. Remove the `setInterval` poll timer. Keep a fallback: on extension activation, do a single initial poll via `ralph-loop.status` + `ralph-loop.taskName` to sync state (the push notification only fires on transitions, not on activation). If `ralph-loop.onStateChange` registration fails (ralph-loop not installed), fall back to the current 5-second polling. Depends on Task 93.

- [x] **Task 100 — Rich state via getStateSnapshot**: When Task 94 is available, replace the two-call pattern (`ralph-loop.status` + `ralph-loop.taskName`) with a single `ralph-loop.getStateSnapshot` call that returns `{ state, taskId, taskDescription, iterationCount, nudgeCount }`. Use `iterationCount` and `nudgeCount` in the tooltip for richer diagnostics. Depends on Task 94.

### Visual Summary (After Phase 13)

| State | Status Bar Text |
|-------|----------------|
| Active (Anthropic, NB), ralph running | `⟳ Claude Opus 4 • 30x • NB/PR:0 • E:high • Ralph: ↻ T3` |
| Active (OpenAI, DEF), ralph running | `⟳ GPT-4o • 1x • DEF/PR:12 • E:xhigh • Ralph: ↻ T5` |
| Idle (Anthropic, DEF), ralph paused | `✓ Claude Opus 4 • 12s • 30x • DEF/PR:6↑ • E:high • Ralph: ⏸ T3` |
| Idle (OpenAI, DEF), ralph idle | `✓ GPT-4o • 8s • 1x • DEF/PR:12— • E:default • Ralph: ○ T12` |
| Active (Gemini, NB), no ralph | `⟳ Gemini 2.5 • 1x • NB/PR:0` *(no effort tag, no ralph)* |
| Idle, no ralph-loop | `✓ Claude Opus 4 • 5s • 30x • NB/PR:0↑ • E:high` |

**Ralph icons**: `↻` $(sync~spin) = running (animated), `⏸` $(debug-pause) = paused, `○` $(circle-outline) = idle

**Click behavior**: Opens quick pick with all allowed values for the current model's provider. Current value marked. Selection updates the VS Code setting immediately.

### 13d — Task Detail Hover

- [x] **Task 101 — Rich task hover tooltip**: When hovering on the Ralph status bar section, show a `MarkdownString` tooltip with details about the current task being processed. Use `ralph-loop.getStateSnapshot` (Task 94) to fetch: task ID, task description, iteration count, nudge count, and current state. Format as a compact floating summary, e.g.: `**Ralph Loop** \n State: Running \n Task: Task-111 \n Description: Implement context thrashing detection \n Iterations: 3 \n Nudges: 1`. If ralph-loop is idle, show the last completed task info. If ralph-loop is not installed, omit the Ralph section from the tooltip entirely. Depends on Task 100 (rich state via getStateSnapshot).

---

## Phase 14 — Wave Agent Tool Wiring

> The wave research pipeline agents (`wave-researcher`) do not declare `github_repo` in their `tools:` frontmatter, despite it being used in actual research runs (evidenced by `methodology: wave-explore-fast-direct x12 + github_repo + crawl4ai` in sealed research frontmatter). The tool is available in the environment but not explicitly declared, so there is no guarantee the agent will use it or that it will be available. Phase 14 fixes this gap.

### 14a — GitHub Repo Tool Declaration

- [x] **Task 102 — Add github_repo to wave-researcher tools**: In `vscode-config-files/agents.source/wave-researcher.agent.md`, add `github_repo` to the `tools:` frontmatter list: `tools: [read, search, edit/createFile, github_repo]`. Verify the symlink in `ralph-loop/agents/` resolves correctly after editing the canonical file. Also add a brief instruction in the agent body: "When research questions reference GitHub repositories or when search results point to open-source repos, use the `github_repo` tool to inspect source code directly." **Verification**: run `grep 'github_repo' agents/wave-researcher.agent.md` — expect match in tools line and in body text.

- [x] **Task 103 — Add github_repo to ralph-research tools**: In `vscode-config-files/agents.source/ralph-research.agent.md`, add `github_repo` to the `tools:` list alongside existing web tools: `tools: [search, read/readFile, read/problems, web, 'crawl4ai/*', 'searxng-search/*', github_repo]`. The ralph-research agent already has web search capabilities — `github_repo` complements these for GitHub-specific source code inspection. **Verification**: run `grep 'github_repo' agents/ralph-research.agent.md` — expect match.

## Phase 15 — PRD Annotation Strictness

- [x] **Task 104 — Fix naive annotation substring matching in prd parser**: In `src/prd.ts` lines 56-57, replace `line.includes(...)` checks with regexes anchored to annotation position only. Full spec with exact code changes and regex patterns: `docs/specs/phase15-annotation-strictness.md`. Add tests. Run `npx tsc --noEmit && npx vitest run` — all must pass.

- [x] **Task 105 — Extract annotation parser helper**: Extract annotation detection from `src/prd.ts` into a `parseLineAnnotations()` helper returning `{ decomposed, checkpoint }`. See `docs/specs/phase15-annotation-strictness.md` for interface and implementation details. Unit test directly. Run `npx tsc --noEmit && npx vitest run` — all must pass.

- [x] **Task 106 — Verify [DECOMPOSED] in description is not skipped**: This task mentions [DECOMPOSED] in its title on purpose. If the parser still skips it, Task 104 failed. Write a test in `test/prd.test.ts` that parses a PRD line like `- [ ] **Task N — Verify [DECOMPOSED] in description**:` and asserts it appears in the parsed output as a pending task, not silently dropped. Run `npx vitest run` — all must pass.

- [x] **Task 107 — Verify [CHECKPOINT] in description does not flag checkpoint**: This task mentions [CHECKPOINT] in its title on purpose. If the parser incorrectly sets `checkpoint: true` on it, Task 104 failed. Write a test in `test/prd.test.ts` that parses a PRD line like `- [ ] **Task N — Verify [CHECKPOINT] in description**:` and asserts it is parsed with `checkpoint` being `undefined` or falsy — not `true`. Run `npx vitest run` — all must pass.

- [x] **Task 108 — Verify both [DECOMPOSED] and [CHECKPOINT] coexist in description safely**: A task referencing both tags in description text. Neither should trigger annotation behavior. Write a test in `test/prd.test.ts` that parses `- [ ] **Task N — mentions [DECOMPOSED] and [CHECKPOINT] in text**:` and asserts it appears as a normal pending task with `checkpoint` not set. Run `npx vitest run` — all must pass.

---

## Phase 16 — Startup Latency & Preflight Transparency

> Specs: `research/16-phase16-refined-tasks.md` | Research: `research/15-phase16-deep-research.md`
> **TDD is MANDATORY** — every implementation task must have tests written before or alongside the implementation. No task may be marked complete without passing tests.
> **Problem**: Ralph currently runs heavyweight synchronous validation (`npx tsc --noEmit` + `npx vitest run`) in the VS Code extension host before ordinary task start, causing multi-minute silent startup stalls, `node (vitest …)` worker swarms, and poor system stability.

### 16a — Bearings Cost Control

- [x] **Task 109 — Bearings Policy Split**: Replace the current boolean bearings behavior with stage-aware policy so Ralph no longer runs full `tsc` + full `vitest` as an opaque default before every task. Default to `startup: tsc`, `perTask: none`, `checkpoint: full` while keeping stronger validation available where it matters. → Spec: `research/16-phase16-refined-tasks.md` L30-L52

- [x] **Task 110 — CHECKPOINT: Bearings Policy Verification**: Verify startup no longer triggers full-suite validation by default and that checkpoints still run the configured stronger validation path. → Spec: `research/16-phase16-refined-tasks.md` L54-L69

### 16b — Non-Blocking Verification

- [x] **Task 111 — Async Verification Runner**: Replace synchronous `execSync()` bearings execution with async process spawning/streaming so the VS Code extension host stays responsive while preflight validation runs. → Spec: `research/16-phase16-refined-tasks.md` L71-L89

- [x] **Task 112 — CHECKPOINT: Non-Blocking Verification Verification**: Prove the extension host remains responsive during startup verification and that users see progress before verification completes. → Spec: `research/16-phase16-refined-tasks.md` L91-L106

### 16c — Cache & Dirty-Aware Skip

- [x] **Task 113 — Verification Cache & Dirty-Aware Skip**: Add cached last-green verification metadata and dirty-aware invalidation so expensive validation is skipped when no relevant files/config changed. → Spec: `research/16-phase16-refined-tasks.md` L108-L125

- [x] **Task 114 — CHECKPOINT: Cache / Dirty-Skip Verification**: Verify repeated task starts stay cheap on an unchanged workspace and that dirty conditions correctly invalidate the cache. → Spec: `research/16-phase16-refined-tasks.md` L127-L142

### 16d — Startup UX Transparency

- [x] **Task 115 — Startup UX Transparency**: Add explicit bearings lifecycle events/status (`BearingsStarted`, `BearingsProgress`, `BearingsCompleted`, `BearingsSkipped`) and surface them immediately in logs/UI so startup is visible, understandable, and cancellable rather than silently stalling. → Spec: `research/16-phase16-refined-tasks.md` L144-L166

- [x] **Task 116 — CHECKPOINT: Startup DX Verification**: Final gate proving Ralph startup is transparent, bounded, and stable: no silent 1–2 minute wait, no default full `vitest` worker swarm on ordinary task start, and clear logs explaining what is running or why it was skipped. → Spec: `research/16-phase16-refined-tasks.md` L168-L184

---

## Phase 17 — Feature Branch Enforcement

> Research: `research/_wave/2026-03-21-ralph-loop-feature-branch-enforcement/FINAL-REPORT.md`
> **Problem**: Ralph-loop is branch-blind — `atomicCommit` commits to whatever HEAD is active (including `main`), with no branch creation, validation, or protection. Users cannot verify agent work before it lands on the default branch.
> **Solution**: Before processing a PRD, create a feature branch derived from the PRD title, commit all work there, and let the user merge/push after review.
> **Architecture**: Core enforcement in orchestrator (not hooks — hooks are fault-tolerant, this is a precondition). Optional config for backward compatibility. No polling, no global mutable state.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.

### 17a — Git Branch Primitives

- [x] **Task 117 — Git Branch Helper Layer**: In `src/gitOps.ts`, add three pure helper functions: (1) `getCurrentBranch(workspaceRoot: string): Promise<string>` — runs `git rev-parse --abbrev-ref HEAD`, returns branch name or `'HEAD'` for detached state; (2) `createAndCheckoutBranch(workspaceRoot: string, branchName: string): Promise<{ success: boolean; error?: string }>` — runs `git checkout -b {branchName}`, returns result; (3) `branchExists(workspaceRoot: string, branchName: string): Promise<boolean>` — runs `git rev-parse --verify refs/heads/{branchName}`, returns true/false. All use the existing `runGit` helper. No new dependencies. Write tests FIRST in `test/gitOps.test.ts`: getCurrentBranch returns branch name, createAndCheckoutBranch succeeds, branchExists detects existing/missing branches. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 118 — PRD Title Extraction and Branch Name Derivation**: In `src/prd.ts`, add `parsePrdTitle(prdContent: string): string | undefined` — extracts the H1 heading text (first line matching `^# (.+)$`). Add `deriveBranchName(title: string): string` — slugifies the title: lowercase, replace non-alphanumeric with `-`, collapse consecutive `-`, trim `-` from ends, truncate to 50 chars, prepend `ralph/` prefix. Result: `ralph/<slugified-title>`. If no H1 found, fall back to `ralph/prd-run-<timestamp>`. Write tests FIRST in `test/prd.test.ts`: title extracted from H1, slugification handles special chars, length truncation works, fallback when no H1, prefix always present. Run `npx tsc --noEmit` and `npx vitest run`.

### 17b — Startup Branch Enforcement

- [x] **Task 119 — Feature Branch Config Option**: In `src/types.ts`, add `featureBranch?: { enabled: boolean; protectedBranches?: string[] }` to `RalphConfig`. Default: `{ enabled: true, protectedBranches: ['main', 'master'] }`. In `DEFAULT_CONFIG`, set `featureBranch: { enabled: true, protectedBranches: ['main', 'master'] }`. In `package.json`, add `ralph-loop.featureBranch.enabled` (boolean, default true) and `ralph-loop.featureBranch.protectedBranches` (array of strings, default `['main', 'master']`) to the VS Code settings contributions. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 120 — Startup Branch Gate in Orchestrator**: In `src/orchestrator.ts`, inside `runLoop()`, after the PRD validation block and before the `while (true)` loop, add a branch enforcement gate (fires once per loop run). When `config.featureBranch.enabled` is true: (1) read PRD content, derive expected branch name via `deriveBranchName(parsePrdTitle(...))`, (2) get current branch via `getCurrentBranch()`, (3) if current branch is in `protectedBranches` list, auto-create and checkout the derived feature branch via `createAndCheckoutBranch()`, (4) if branch already exists (from a previous run), checkout it instead of creating, (5) if current branch is already the expected feature branch, proceed normally, (6) if branch creation/checkout fails, yield `LoopEventKind.BranchEnforcementFailed` and return (hard stop — do not proceed without branch safety). Store the active branch name in orchestrator state for later use. Write tests FIRST in `test/orchestrator.test.ts`: creates branch from main, reuses existing branch, proceeds on correct branch, stops on failure, skipped when disabled. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 121 — CHECKPOINT: Branch Enforcement Verification**: Clean compile, full vitest green. Starting a ralph-loop run on `main` auto-creates `ralph/<prd-slug>` branch. Starting on an existing feature branch reuses it. Starting on the correct branch proceeds without action. `featureBranch.enabled: false` skips all branch logic. Protected branch list is configurable. No new polling, timers, or global state introduced.

### 17c — Branch Observability

- [x] **Task 122 — Branch Events and StateSnapshot Extension**: In `src/types.ts`, add `LoopEventKind.BranchCreated` (`{ branchName: string }`), `LoopEventKind.BranchValidated` (`{ branchName: string }`), and `LoopEventKind.BranchEnforcementFailed` (`{ reason: string }`) to the event union. Add optional `branch?: string` to `StateSnapshot`. In `src/extension.ts`, handle the new events in the existing switch-case: `BranchCreated` → log info + show notification, `BranchValidated` → log info, `BranchEnforcementFailed` → show error notification. Include `branch` in the `ralph-loop.getStateSnapshot` command response. In status bar tooltip, show current branch when available. Write tests: events yielded correctly, StateSnapshot includes branch. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 123 — AtomicCommit Branch Guard (Defense in Depth)**: In `src/gitOps.ts`, at the top of `atomicCommit()`, add a defensive branch check: read current branch, if it's in the protected branches list (`main`, `master`), log a warning and return `{ success: false, error: 'Refusing to commit on protected branch' }`. This is a backstop — the startup gate should have already switched branches, but this prevents accidental commits if the branch is manually switched mid-loop. The protected branches list is passed as a parameter (not hardcoded). Write tests FIRST in `test/gitOps.test.ts`: commit blocked on main, allowed on feature branch, warning logged. Run `npx tsc --noEmit` and `npx vitest run`.

### 17d — Resume and Cleanup

- [x] **Task 124 — Persist Branch in Session State**: In `src/sessionPersistence.ts`, add `branchName?: string` to `SerializedLoopState`. In `save()`, include the current branch name. In `load()`, validate that the current git branch matches the stored branch — if mismatched, return the state with a `branchMismatch: true` flag so the orchestrator can decide whether to switch back or warn. In `src/extension.ts`, when resuming a session with `branchMismatch`, show a warning: "Session was on branch '{stored}' but current branch is '{current}'. Switch back?" with options Resume (switch back) / Continue (use current) / Discard. Write tests FIRST in `test/sessionPersistence.test.ts`: branch saved and loaded, mismatch detected, missing branch field handled gracefully. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 125 — CHECKPOINT: Feature Branch Enforcement E2E**: Full end-to-end verification. (1) Start ralph-loop on `main` → creates `ralph/<prd-slug>` branch, all commits land there, `main` untouched. (2) Stop and resume → session correctly restores branch context. (3) Complete all tasks → branch contains full commit history, user can `git log ralph/<slug>` to review, then `git merge` or discard. (4) `featureBranch.enabled: false` → commits go to current branch as before (backward compatible). (5) No regressions in existing tests. Clean compile, full vitest green.

---

## Phase 18 — Simplify Branch Enforcement (Linear Model)

> Research: `research/_wave/2026-03-21-ralph-branch-vs-copilot-patterns/FINAL-REPORT.md`
> **Problem**: Phase 17's branch gate uses a 3-way conditional (protected → create, matching → proceed, else → silently adopt) that silently adopts arbitrary branches, has no dirty-state handling, requires a `protectedBranches` config concept, and breaks on non-main starting branches like `bisect/v0.39-lean`. The `atomicCommit` branch guard is dead code (never receives `protectedBranches`). Session persistence tracks `branchMismatch` but the orchestrator ignores it.
> **Solution**: Replace the 3-way gate with a linear "always create branch from current HEAD" model, inspired by GitHub Copilot's background agent pattern. Always create `ralph/<slug>-<short-hash>`, handle dirty state, track `originalBranch`, switch back when done. Works identically from any starting branch.
> **Architecture**: Linear flow (no conditional arms), hash-suffixed branch names (no collisions), dirty-state commit on the new branch (not the original), `originalBranch` in session persistence for switch-back. Removes `protectedBranches` config, removes `atomicCommit` branch guard, removes `branchMismatch` flag. Net reduction in code paths, config surface, and edge cases.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.

### 18a — New Branch Primitives

- [x] **Task 126 — Add `getShortHash` Helper**: In `src/gitOps.ts`, add `getShortHash(workspaceRoot: string): Promise<string>` — runs `git rev-parse --short HEAD`, returns 7-char hex hash. Write tests FIRST in `test/gitOps.test.ts`: returns a hex string of expected length. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 127 — Add `checkoutBranch` Helper**: In `src/gitOps.ts`, add `checkoutBranch(workspaceRoot: string, branchName: string): Promise<{ success: boolean; error?: string }>` — runs `git checkout {branchName}` (without `-b`). If it doesn't already exist, add it. Write tests FIRST. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 128 — Add `hasDirtyWorkingTree` Helper**: In `src/gitOps.ts`, add `hasDirtyWorkingTree(workspaceRoot: string): Promise<boolean>` — runs `git status --porcelain`, returns `true` if output is non-empty (tracked changes or untracked files). Write tests FIRST in `test/gitOps.test.ts`. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 129 — Update `deriveBranchName` to Accept Hash Suffix**: In `src/prd.ts`, change `deriveBranchName(title: string, shortHash?: string): string` — when `shortHash` is provided, append `-{shortHash}` after the slug (before truncation limit). Adjust the slug max length from 50 to 44 chars to leave room for `-` + 7-char hash. Existing tests must still pass (hash is optional). Add new tests: hash is appended, total length stays within limit, different hashes produce different branch names. Run `npx tsc --noEmit` and `npx vitest run`.

### 18b — Replace Branch Gate with Linear Flow

- [x] **Task 130 — Rewrite Orchestrator Branch Gate**: In `src/orchestrator.ts`, replace the 3-way branch enforcement block with a linear flow: (1) `originalBranch = getCurrentBranch()` — save for later switch-back, (2) derive branch name via `deriveBranchName(parsePrdTitle(prdContent), getShortHash())`, (3) `createAndCheckoutBranch(derivedName)` — always create new branch from current HEAD (dirty state carried over automatically by git), (4) if dirty working tree → `git add -A && git commit -m "wip: snapshot before ralph run"` on the new branch, (5) yield `BranchCreated` event, (6) if branch creation fails → yield `BranchEnforcementFailed` and return. Remove the `protectedBranches` check, the "matching branch" check, and the silent-adoption else arm. Store `originalBranch` in orchestrator state alongside `activeBranch`. Write tests FIRST in `test/orchestrator.test.ts`: creates branch from main, creates branch from `bisect/v0.39-lean`, creates branch from any arbitrary branch, handles dirty state, fails gracefully, skipped when disabled. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 131 — Remove `protectedBranches` Config**: In `src/types.ts`, remove `protectedBranches?: string[]` from `featureBranch` config. Simplify to `featureBranch?: { enabled: boolean }`. Update `DEFAULT_CONFIG` accordingly. In `package.json`, remove the `ralph-loop.featureBranch.protectedBranches` VS Code setting contribution. Update all references. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 132 — Remove `atomicCommit` Branch Guard**: In `src/gitOps.ts`, remove the protected-branch check at the top of `atomicCommit()`. Remove the `protectedBranches` parameter. The linear branch gate makes this guard redundant — work always happens on a ralph/ branch. Remove corresponding tests in `test/gitOps.test.ts` (commit blocked on main, warning logged). Update all `atomicCommit` call sites. Run `npx tsc --noEmit` and `npx vitest run`.

### 18c — Session Persistence & Switch-Back

- [x] **Task 133 — Persist `originalBranch` in Session State**: In `src/sessionPersistence.ts`, add `originalBranch?: string` to `SerializedLoopState`. In `save()`, include the original branch name alongside the active ralph/ branch. In `load()`, return `originalBranch` for the orchestrator to use on switch-back. Remove the `branchMismatch` flag — it's no longer needed since branch identity is derived from the hash, not matched against expectations. Write tests FIRST in `test/sessionPersistence.test.ts`: originalBranch saved and loaded, missing field handled gracefully. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 134 — Switch Back to Original Branch on Loop Completion**: In `src/orchestrator.ts`, after the loop completes (all tasks done or user stops), `git checkout originalBranch` to return to where the user started. Yield a new `LoopEventKind.BranchSwitchedBack` event (`{ from: string, to: string }`). In `src/extension.ts`, handle the event: show info notification "Ralph finished — switched back to {originalBranch}. Review changes on {ralphBranch}." If switch-back fails (branch deleted, conflicts), log a warning but don't crash — the ralph/ branch still has all the work. Write tests FIRST. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 135 — Remove `branchMismatch` UI Handling**: In `src/extension.ts`, remove the `branchMismatch` dialog (Resume/Continue/Discard). When resuming a session, the orchestrator simply checks out the stored ralph/ branch — no user prompt needed, since the ralph/ branch is self-contained. Simplify the resume flow. Run `npx tsc --noEmit` and `npx vitest run`.

### 18d — Cleanup & Verification

- [x] **Task 136 — Remove `BranchValidated` Event**: In `src/types.ts`, remove `LoopEventKind.BranchValidated` — no longer needed since we always create a new branch (never validate an existing one). Remove handling in `src/extension.ts`. Update tests. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 137 — CHECKPOINT: Linear Branch Model E2E**: Full end-to-end verification. (1) Start on `main` → creates `ralph/<slug>-<hash>`, all commits land there, `main` untouched. (2) Start on `bisect/v0.39-lean` → creates `ralph/<slug>-<hash>` from that HEAD, `bisect/v0.39-lean` untouched. (3) Start on any arbitrary branch → same behavior. (4) Dirty working tree → committed on ralph/ branch as WIP, original branch clean. (5) Loop completes → switches back to original branch. (6) Resume after stop → checks out stored ralph/ branch, continues. (7) `featureBranch.enabled: false` → commits go to current branch (backward compatible). (8) No `protectedBranches` config needed. (9) No regressions in existing tests. Clean compile, full vitest green.

---

## Phase 19 — Status Bar Idle/Processing Consistency

> Spec: `research/17-phase19-deep-research.md`
> **Problem**: Split-brain status bug where the visible bar can remain on `processing` while `ralph-loop.status` already reports `idle`.
> **Solution**: Pair a Ralph-side guaranteed idle refresh with a fork-side precedence model, then harden terminal sequencing, abnormal exits, stop responsiveness, and regressions so the bar converges to idle across normal, failed, resumed, and consecutive runs.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.

### 19.1 — Make Idle Authoritative

- [x] **Task 138 — Add a shared final idle refresh path in `src/extension.ts`**: Guarantee one authoritative Ralph idle refresh after every `await orchestrator.start()` settlement, including crashes and auto-resume. → Spec: `research/17-phase19-deep-research.md` L33-L51

- [x] **Task 139 — Enforce fork-side precedence so Ralph idle beats lifecycle processing**: Prevent a later `RequestLifecycleModel` `Processing` update from overwriting a correct Ralph idle push in the Copilot fork status bar. → Spec: `research/17-phase19-deep-research.md` L53-L72

- [x] **Task 140 — Make terminal snapshots truthful in `src/orchestrator.ts`**: Ensure terminal event handlers stop reading stale `running` snapshots after the loop has already logically ended. → Spec: `research/17-phase19-deep-research.md` L74-L92

### 19.2 — Backfill Edge Cases and Lock Regressions

- [x] **Task 141 — Backfill abnormal exits and resumed cleanup through the shared idle path**: Ensure non-happy exits also converge the UI to idle instead of relying on happy-path terminal events. → Spec: `research/17-phase19-deep-research.md` L96-L115

- [x] **Task 142 — Make `delay()` abort-aware so stop requests collapse stale UI windows**: Reduce stop-path lag by making orchestrator waits terminate promptly when the loop is stopping. → Spec: `research/17-phase19-deep-research.md` L117-L134

- [x] **Task 143 — Add regression coverage for normal, failure, stop, resume, and consecutive-run scenarios**: Lock in the status-bar convergence contract so the bar, snapshots, and status command stay aligned across all reported exit paths. → Spec: `research/17-phase19-deep-research.md` L136-L157

---

## Phase 21 — Cross-Repo Parallel PRD Execution

> **Problem**: ralph-loop only executes tasks from a single `PRD.md` in the workspace root. In multi-root VS Code workspaces (e.g., `ralph-loop` + `vscode-copilot-chat` + `opencode-patches`), each repo has its own `PRD.md` with independent task queues. Currently there is no way to orchestrate tasks across repos — the user must manually switch context and run ralph separately in each workspace folder.
> **Goal**: Enable ralph-loop to discover, track, and execute PRD tasks from multiple independent repos in a single session, treating each repo's PRD as an isolated execution lane — no shared state, no cross-repo dependencies, independent branch management per repo.
> **Constraint**: This is NOT subagent parallelism or wave-style parallel research. Each repo is a fully independent execution context with its own PRD.md, branch, working directory, and progress.txt.
> **TDD is MANDATORY**. Run `npx tsc --noEmit` and `npx vitest run` — ALL tests must pass before marking ANY checkbox.

### 21a — Multi-PRD Discovery and Data Model

- [x] **Task 156 — Define `RepoLane` Type and Multi-PRD Config**: In `src/types.ts`, add `RepoLane` interface: `{ repoId: string, workspaceFolder: string, prdPath: string, progressPath: string, enabled: boolean }`. Add `repos?: RepoLane[]` to `RalphLoopConfig`. When `repos` is undefined or empty, behavior is identical to current single-PRD mode (backward compatible). When populated, each entry is an independent execution lane. Add `activeRepoId: string` to orchestrator runtime state to track which lane is currently executing. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 157 — Discover PRD Files from Multi-Root Workspace Folders**: In `src/extension.ts`, when ralph-loop activates in a multi-root workspace, scan all `vscode.workspace.workspaceFolders` for `PRD.md` files. Auto-populate `repos` config from discovered folders: `repoId` = folder name, `workspaceFolder` = folder path, `prdPath` = `{folder}/PRD.md`, `progressPath` = `{folder}/progress.txt`, `enabled` = `true`. Show a quick-pick allowing the user to select which repos to include. Store selections in workspace-scoped settings. Run `npx tsc --noEmit` and `npx vitest run`.

- [x] **Task 158 — Extend PRD Parser for Multi-Lane Snapshots**: In `src/prd.ts`, add `parseMultiPrd(lanes: RepoLane[]): Map<string, PrdSnapshot>` that reads each lane's PRD file and returns a map keyed by `repoId`. Each snapshot is independent — task IDs are scoped to their repo (`repoId:taskId`). Add `repoId` to `Task` interface as an optional field (undefined = legacy single-PRD mode). Run `npx tsc --noEmit` and `npx vitest run`.

### 21b — Lane-Isolated Orchestration

- [x] **Task 159 — Implement Round-Robin Lane Scheduler**: In `src/orchestrator.ts`, add a lane scheduler that rotates between enabled repos. When multi-PRD mode is active: (1) parse all lane PRDs, (2) build a merged task queue with lane-scoped task IDs, (3) execute one task per lane in round-robin order, (4) each task execution uses the lane's `workspaceFolder` as CWD for all git ops and file watchers, (5) switching lanes resets the file watcher root. The scheduler must handle lanes completing at different rates — skip lanes with no remaining tasks. Run `npx tsc --noEmit` and `npx vitest run`.

- [ ] **Task 160 — Lane-Isolated Branch Management**: Extend the Phase 18 linear branch model to operate per-lane. Each lane gets its own `ralph/<slug>-<hash>` branch derived from the lane's repo HEAD, not from the workspace root. `originalBranch` is tracked per lane in `SessionPersistence`. On loop completion, each lane switches back to its own original branch independently. A failure in one lane's branch management must not affect other lanes. Run `npx tsc --noEmit` and `npx vitest run`.

- [ ] **Task 161 — Lane-Isolated Prompt Building**: Extend `buildPrompt()` to accept a `RepoLane` parameter. The prompt uses the lane's PRD content, the lane's progress.txt, and the lane's workspace folder for file references. When firing the prompt to Copilot, set the working directory context to the lane's folder. The agent must be told which repo it's working in: add `"WORKSPACE: {repoId} ({workspaceFolder})"` to the prompt header. Run `npx tsc --noEmit` and `npx vitest run`.

### 21c — Status and Progress Tracking

- [ ] **Task 162 — Multi-Lane Status Bar Display**: Extend `src/statusBar.ts` to show the active lane in the status bar: `ralph • {repoId} • {state} • Task {N}/{total}`. The tooltip shows all lanes with their individual progress: `repo-a: 3/5 done | repo-b: 7/12 done | repo-c: idle`. When a lane completes all tasks, show `✓` next to it. The status bar click menu allows selecting a specific lane to focus on (filters the view to that lane). Run `npx tsc --noEmit` and `npx vitest run`.

- [ ] **Task 163 — Per-Lane Progress Tracking**: Each lane writes to its own `progress.txt` in its own workspace folder. The orchestrator must ensure progress entries include the `repoId` prefix for correlation: `[{timestamp}] [{invocationId}] [{repoId}] [Task-{N}] ...`. The `ralph-loop.status` command output includes a per-lane summary table. Run `npx tsc --noEmit` and `npx vitest run`.

- [ ] **Task 164 — Per-Lane Session Persistence**: Extend `src/sessionPersistence.ts` to save/restore per-lane state: task progress, branch info, nudge/retry counts, and invocation IDs for each `RepoLane`. On resume, each lane picks up where it left off independently. A corrupted lane state must not prevent other lanes from resuming. Run `npx tsc --noEmit` and `npx vitest run`.

### 21d — Verification

- [ ] **Task 165 — CHECKPOINT: Cross-Repo Parallel PRD Execution**: Clean compile, full vitest green. End-to-end verification proves: (1) multi-root workspace with 2+ PRD.md files discovers and lists all repos, (2) user can select which repos to include, (3) tasks execute in round-robin across repos, (4) each repo gets its own branch, (5) each repo's progress.txt is independent, (6) failing a task in repo-a does not block repo-b, (7) status bar shows active lane and per-repo progress, (8) session persistence and resume work per-lane, (9) single-PRD mode (no `repos` config) is backward compatible with zero behavior change, (10) `ralph-loop.status` command shows multi-repo task summary.
