{
	"name": "ralph-loop",
	"displayName": "Ralph Loop — Autonomous PRD Loop",
	"description": "Drives Copilot Agent Mode in a deterministic loop from PRD tasks.",
	"version": "0.5.11",
	"publisher": "ralph-loop",
	"license": "MIT",
	"engines": {
		"vscode": "^1.93.0"
	},
	"categories": [
		"AI",
		"Other"
	],
	"activationEvents": [
		"workspaceContains:PRD.md"
	],
	"main": "./out/src/extension.js",
	"bin": {
		"ralph": "./out/cli/ralph.js"
	},
	"contributes": {
		"commands": [
			{
				"command": "ralph-loop.start",
				"title": "Ralph Loop: Start"
			},
			{
				"command": "ralph-loop.stop",
				"title": "Ralph Loop: Stop"
			},
			{
				"command": "ralph-loop.status",
				"title": "Ralph Loop: Show Status"
			},
			{
				"command": "ralph-loop.chatSend",
				"title": "Ralph Loop: Send to Chat"
			}
		],
		"configuration": {
			"title": "Ralph Loop",
			"properties": {
				"ralph-loop.prdPath": {
					"type": "string",
					"default": "PRD.md",
					"description": "Path to the PRD file (relative to workspace root)."
				},
				"ralph-loop.progressPath": {
					"type": "string",
					"default": "progress.txt",
					"description": "Path to the progress tracking file."
				},
				"ralph-loop.maxIterations": {
					"type": "number",
					"default": 50,
					"description": "Maximum number of loop iterations (0 = unlimited)."
				},
				"ralph-loop.countdownSeconds": {
					"type": "number",
					"default": 12,
					"description": "Seconds to wait between tasks."
				},
				"ralph-loop.cooldownShowDialog": {
					"type": "boolean",
					"default": true,
					"description": "Show an auto-accepting dialog during inter-task countdown with Pause/Stop/Edit buttons."
				},
				"ralph-loop.inactivityTimeoutMs": {
					"type": "number",
					"default": 300000,
					"description": "Milliseconds of inactivity before moving to next task."
				},
				"ralph-loop.promptTemplate": {
					"type": "string",
					"default": "",
					"description": "Custom prompt template with {{variable}} placeholders. Variables: task, prd, progress, learnings, workspace, taskId, iterationNumber. Built-in ROLE & BEHAVIOR, TDD GATE, and safety sections are always prepended.",
					"editPresentation": "multilineText"
				},
				"ralph-loop.preset": {
					"type": "string",
					"enum": [
						"general",
						"feature",
						"bugfix",
						"refactor"
					],
					"default": "general",
					"description": "Workflow preset that applies smart defaults for common scenarios. Presets set config values that can still be overridden individually."
				},
				"ralph-loop.featureBranch.enabled": {
					"type": "boolean",
					"default": true,
					"description": "Enable automatic feature branch creation and enforcement."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"test": "vitest run",
		"test:watch": "vitest",
		"package": "npm test && vsce package --no-dependencies --allow-missing-repository"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"@types/vscode": "^1.93.0",
		"typescript": "^5.4.0",
		"vitest": "^3.2.4"
	}
}
